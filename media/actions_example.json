[
  {
    "id": "action.simple.echo",
    "title": "Echo Message (Live Stream)",
    "action": {
      "description": "Run a single shell command that streams output to the terminal.",
      "tasks": [
        {
          "id": "echo_task",
          "type": "shell",
          "command": "echo Hello World from TaskHub!"
        }
      ]
    }
  },
  {
    "id": "action.shell.advanced",
    "title": "Shell: With Env, Cwd, Args",
    "action": {
      "description": "Demonstrate env, cwd, and args properties for shell tasks.",
      "tasks": [
        {
          "id": "run_with_env",
          "type": "shell",
          "command": {
            "windows": "echo %MY_VAR% & echo %BUILD_MODE%",
            "macos": "echo $MY_VAR && echo $BUILD_MODE",
            "linux": "echo $MY_VAR && echo $BUILD_MODE"
          },
          "args": ["--verbose"],
          "cwd": "${workspaceFolder}",
          "env": {
            "MY_VAR": "hello",
            "BUILD_MODE": "debug"
          },
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.pipeline.example",
    "title": "Pipeline: Select File & Save Output",
    "action": {
      "description": "Select a file, echo the path, and save the result to disk.",
      "tasks": [
        {
          "id": "select_file",
          "type": "fileDialog"
        },
        {
          "id": "echo_file",
          "type": "shell",
          "command": "echo Selected file: ${select_file.path}",
          "passTheResultToNextTask": true,
          "output": {
            "mode": "file",
            "filePath": "${workspaceFolder}/pipeline_output.txt",
            "overwrite": true
          }
        }
      ]
    }
  },
  {
    "id": "action.oneshot.example",
    "title": "Run GUI App (One-Shot)",
    "action": {
      "description": "Launch a GUI application without waiting for it to exit.",
      "tasks": [
        {
          "id": "run_notepad",
          "type": "shell",
          "command": {
            "windows": "notepad.exe"
          },
          "isOneShot": true
        }
      ]
    }
  },
  {
    "id": "action.command.vscode",
    "title": "VS Code Command: Toggle Minimap",
    "action": {
      "description": "Execute a VS Code built-in command. 'command' type runs vscode commands directly.",
      "tasks": [
        {
          "id": "toggle_minimap",
          "type": "command",
          "command": "editor.action.toggleMinimap"
        }
      ]
    }
  },
  {
    "id": "action.confirm.example",
    "title": "Confirm: Dangerous Operation",
    "action": {
      "description": "Ask the user for confirmation before proceeding with a risky operation.",
      "tasks": [
        {
          "id": "select_target",
          "type": "folderDialog",
          "options": {
            "openLabel": "Select target folder",
            "title": "Choose folder to clean"
          }
        },
        {
          "id": "confirm_delete",
          "type": "confirm",
          "message": "Are you sure you want to clean ${select_target.path}?",
          "confirmLabel": "Clean",
          "cancelLabel": "Abort"
        },
        {
          "id": "do_clean",
          "type": "shell",
          "command": "echo Cleaning ${select_target.path} ...",
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.filedialog.rich",
    "title": "FileDialog: All Output Variables",
    "action": {
      "description": "Show all output variables from fileDialog: path, dir, name, fileNameOnly, fileExt.",
      "tasks": [
        {
          "id": "pick_file",
          "type": "fileDialog",
          "options": {
            "title": "Select any file",
            "filters": {
              "All Files": ["*"]
            }
          }
        },
        {
          "id": "show_info",
          "type": "shell",
          "command": "echo path: ${pick_file.path} && echo dir: ${pick_file.dir} && echo name: ${pick_file.name} && echo nameOnly: ${pick_file.fileNameOnly} && echo ext: ${pick_file.fileExt}",
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.zip.folder",
    "title": "Zip Folder And Confirm",
    "action": {
      "description": "Select a folder, create a ZIP archive, and announce the result in the terminal.",
      "tasks": [
        {
          "id": "select_folder",
          "type": "folderDialog",
          "options": {
            "openLabel": "Select folder to zip"
          }
        },
        {
          "id": "zip_folder",
          "type": "zip",
          "tool": {
            "windows": "C:\\Program Files\\7-Zip\\7z.exe",
            "macos": "/opt/homebrew/bin/7z"
          },
          "source": "${select_folder.path}",
          "archive": "${select_folder.path}.zip"
        },
        {
          "id": "announce_zip",
          "type": "shell",
          "command": "echo Archive created at: ${zip_folder.archivePath}",
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.string.manipulation",
    "title": "String Manipulation Helpers",
    "action": {
      "description": "Demonstrate all stringManipulation functions on a file path.",
      "tasks": [
        {
          "id": "select_for_string",
          "type": "fileDialog"
        },
        {
          "id": "string_dir",
          "type": "stringManipulation",
          "function": "dirname",
          "input": "${select_for_string.path}"
        },
        {
          "id": "string_filename",
          "type": "stringManipulation",
          "function": "basename",
          "input": "${select_for_string.path}"
        },
        {
          "id": "string_clean_name",
          "type": "stringManipulation",
          "function": "basenameWithoutExtension",
          "input": "${select_for_string.path}"
        },
        {
          "id": "string_ext",
          "type": "stringManipulation",
          "function": "extension",
          "input": "${select_for_string.path}"
        },
        {
          "id": "string_stripped",
          "type": "stringManipulation",
          "function": "stripExtension",
          "input": "${select_for_string.path}"
        },
        {
          "id": "string_upper_name",
          "type": "stringManipulation",
          "function": "toUpperCase",
          "input": "${string_clean_name.output}"
        },
        {
          "id": "string_lower_name",
          "type": "stringManipulation",
          "function": "toLowerCase",
          "input": "${string_clean_name.output}"
        },
        {
          "id": "string_trimmed",
          "type": "stringManipulation",
          "function": "trim",
          "input": "  ${string_clean_name.output}  "
        },
        {
          "id": "report_strings",
          "type": "shell",
          "command": "echo dirname: ${string_dir.output} && echo basename: ${string_filename.output} && echo nameOnly: ${string_clean_name.output} && echo extension: ${string_ext.output} && echo stripExt: ${string_stripped.output} && echo upper: ${string_upper_name.output} && echo lower: ${string_lower_name.output} && echo trimmed: [${string_trimmed.output}]",
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.unzip.inputs",
    "title": "Unzip With Inputs And Direct Destination",
    "action": {
      "description": "Demonstrate how unzip tasks can consume archive/destination from prior steps or direct strings.",
      "tasks": [
        {
          "id": "select_zip_for_unzip",
          "type": "fileDialog",
          "options": {
            "filters": {
              "Archives": ["zip", "7z"]
            }
          }
        },
        {
          "id": "select_destination_for_unzip",
          "type": "folderDialog",
          "options": {
            "openLabel": "Select destination folder (will create subfolder)"
          }
        },
        {
          "id": "unzip_using_task_inputs",
          "type": "unzip",
          "tool": {
            "windows": "C:\\Program Files\\7-Zip\\7z.exe",
            "macos": "/opt/homebrew/bin/7z"
          },
          "inputs": {
            "archive": "select_zip_for_unzip",
            "destination": "select_destination_for_unzip"
          }
        },
        {
          "id": "unzip_using_direct_destination",
          "type": "unzip",
          "tool": {
            "windows": "C:\\Program Files\\7-Zip\\7z.exe",
            "macos": "/opt/homebrew/bin/7z"
          },
          "inputs": {
            "archive": "select_zip_for_unzip"
          },
          "destination": "${workspaceFolder}/.taskhub/unzip-output"
        }
      ]
    }
  },
  {
    "id": "action.inputbox.simple",
    "title": "InputBox: Enter Text",
    "action": {
      "description": "Prompt user for input and echo it",
      "tasks": [
        {
          "id": "input_name",
          "type": "inputBox",
          "prompt": "Enter your name",
          "placeHolder": "John Doe"
        },
        {
          "id": "echo_name",
          "type": "shell",
          "command": "echo Hello, ${input_name.value}!",
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.inputbox.with.prefix",
    "title": "InputBox: With Prefix/Suffix",
    "action": {
      "description": "Demonstrate prefix and suffix in inputBox - user only enters the main value",
      "tasks": [
        {
          "id": "input_args",
          "type": "inputBox",
          "prompt": "Enter arguments (prefix '-g' will be added automatically)",
          "placeHolder": "Test 1234 123",
          "prefix": "-g ",
          "suffix": " --verbose"
        },
        {
          "id": "show_args",
          "type": "shell",
          "command": "echo Full command args: ${input_args.value}",
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.inputbox.password",
    "title": "InputBox: Password Input",
    "action": {
      "description": "Prompt for a secret value with masked input.",
      "tasks": [
        {
          "id": "input_secret",
          "type": "inputBox",
          "prompt": "Enter API key",
          "placeHolder": "sk-...",
          "password": true
        },
        {
          "id": "use_secret",
          "type": "shell",
          "command": "echo API key received (length check only)",
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.quickpick.simple",
    "title": "QuickPick: Select Environment",
    "action": {
      "description": "Select environment from predefined options",
      "tasks": [
        {
          "id": "select_env",
          "type": "quickPick",
          "placeHolder": "Select deployment environment",
          "items": ["dev", "staging", "production"]
        },
        {
          "id": "show_env",
          "type": "shell",
          "command": "echo Selected environment: ${select_env.value}",
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.quickpick.with.description",
    "title": "QuickPick: With Descriptions",
    "action": {
      "description": "Select option with detailed descriptions",
      "tasks": [
        {
          "id": "select_build",
          "type": "quickPick",
          "placeHolder": "Select build type",
          "items": [
            {
              "label": "debug",
              "description": "Debug build with symbols",
              "detail": "Best for development and debugging"
            },
            {
              "label": "release",
              "description": "Optimized release build",
              "detail": "Best for production deployment"
            },
            {
              "label": "profile",
              "description": "Release build with profiling",
              "detail": "For performance analysis"
            }
          ]
        },
        {
          "id": "show_build",
          "type": "shell",
          "command": "echo Build type: ${select_build.value}",
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.quickpick.multiple",
    "title": "QuickPick: Multiple Selection",
    "action": {
      "description": "Select multiple features to enable",
      "tasks": [
        {
          "id": "select_features",
          "type": "quickPick",
          "placeHolder": "Select features to enable (multiple selection)",
          "canPickMany": true,
          "items": ["authentication", "logging", "caching", "monitoring", "analytics"]
        },
        {
          "id": "show_features",
          "type": "shell",
          "command": "echo Enabled features: ${select_features.values}",
          "revealTerminal": "always"
        }
      ]
    }
  },
  {
    "id": "action.run.script.with.params",
    "title": "Complete Example: Run Script with Parameters",
    "action": {
      "description": "Select file, environment, and parameters to run a script",
      "successMessage": "Script executed successfully!",
      "tasks": [
        {
          "id": "select_script",
          "type": "fileDialog",
          "options": {
            "filters": {
              "Scripts": ["js", "py", "sh"]
            }
          }
        },
        {
          "id": "select_environment",
          "type": "quickPick",
          "placeHolder": "Select environment",
          "items": [
            {
              "label": "development",
              "description": "Development environment"
            },
            {
              "label": "staging",
              "description": "Staging environment"
            },
            {
              "label": "production",
              "description": "Production environment"
            }
          ]
        },
        {
          "id": "confirm_run",
          "type": "confirm",
          "message": "Run ${select_script.name} in ${select_environment.value}?"
        },
        {
          "id": "input_port",
          "type": "inputBox",
          "prompt": "Enter port number",
          "value": "3000",
          "placeHolder": "3000"
        },
        {
          "id": "input_extra_args",
          "type": "inputBox",
          "prompt": "Enter extra arguments (optional)",
          "placeHolder": "additional flags",
          "prefix": "--extra "
        },
        {
          "id": "run_script",
          "type": "shell",
          "command": "node ${select_script.path} --env ${select_environment.value} --port ${input_port.value} ${input_extra_args.value}",
          "revealTerminal": "always"
        }
      ]
    }
  }
]
