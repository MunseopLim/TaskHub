# Change Log

<!--
최신 버전부터 기록한다. 한 버전에는 요약 제목 하나만 두고, 같은 릴리스에서 나온 리뷰·후속 수정은
최종 사용자 동작을 기준으로 합친다. 구현 과정이나 변이 테스트 기록은 생략하고 중요한 호환성,
보안·데이터 손실 수정과 사용자 영향만 남긴다. 테스트를 적는다면 최종 수치만 기록한다.
-->

## [0.7.39] - 2026-08-19

### 추가 — 선택값별 태스크를 하나로 구성하는 switch

- 새 `switch` 태스크는 `on`의 보간값과 일치하는 `cases` 하나를 실행한다. QuickPick 선택마다 같은
  `when`을 가진 태스크를 복제하지 않아도 되며, 일치하는 case와 `defaultCase`가 모두 없으면 실패 없이
  건너뛴다.
- case마다 command·shell·문자열 변환·파일 쓰기·압축/해제를 서로 다르게 구성할 수 있다. 바깥 switch의
  id와 공통 실행 설정을 유지하고, 선택한 태스크 결과에 `matched`·`selected`를 더해 다음 태스크에서
  같은 ID로 참조한다.
- 대화형 태스크와 중첩 switch는 case에서 허용하지 않는다. 파일/폴더 선택 전환은 `pathDialog.mode`로
  구성해 프롬프트 흐름을 명시적으로 유지한다. Schema·Doctor·Preview·변수 자동완성은 모든 case의
  설정과 결과 키를 함께 검사·제안한다.

**테스트**: case 선택/불일치 no-op/이종 태스크/defaultCase/조건 skip 경계와 Schema·Doctor·Preview·
자동완성을 포함해 최종 3077 passing / 3 pending.

## [0.7.38] - 2026-08-19

### 추가 — 선택값으로 파일/폴더 대화상자 전환

- 새 `pathDialog` 태스크는 `mode`에 `file`·`folder`·`both` 또는 그 값으로 해석되는 정확한 변수
  참조를 받는다. QuickPick의 value를 `${kind}`로 바로 연결하면 fileDialog/folderDialog 태스크 두 개와
  `when` 조건 두 개를 만들지 않고 하나의 `${target.path}`로 결과를 소비할 수 있다.
- `options.canSelectMany`와 기존 경로 결과(`path`·`paths`·`name`·`names`·`count` 등)를 그대로 지원한다.
  mode가 파일/폴더 허용 옵션보다 우선하며 `both`는 한 대화상자에서 둘 다 허용한다.
- Preview·Doctor·변수 자동완성·History 입력 재사용·prompt 직렬화·다이얼로그 위치 기억이 새 타입을
  기존 파일/폴더 선택기와 같은 interactive 경계로 처리한다. 잘못된 동적 mode 값은 실제 값을 오류에
  노출하지 않고 대화상자를 열기 전에 거부한다.

**테스트**: 세 mode의 실제 OpenDialog 옵션·QuickPick→pathDialog 통합 흐름·스키마·Preview·Doctor·
취소/기억 경계를 포함해 최종 3065 passing / 3 pending.

## [0.7.37] - 2026-08-19

### 추가 — 동적 QuickPick의 label/value 매핑

- 기존 `itemsFromCommand`는 그대로 stdout의 비어 있지 않은 각 줄을 label/value로 사용한다. 선택적으로
  `itemsFromCommandFormat: "jsonl"`을 지정하면 명령이 줄마다 `label`과 선택적 `id`·`description`·
  `detail`·`value` 객체를 출력해, 정적 QuickPick과 같은 표시/실행값 분리를 동적으로 구성할 수 있다.
- JSONL의 배열 `value`는 bare 참조·`.value`에서 argv 여러 칸으로 펼쳐지고 빈 배열은 인자를 만들지
  않는다. `itemsExclude`는 JSONL 항목의 id·label·원문 줄을 제외할 수 있다.
- 동적 객체는 허용된 필드만 새 객체로 옮기며 잘못된 줄은 원문 대신 줄 번호와 필드만 오류로 알려 준다.
  Preview Run은 적용 형식을 표시하고, 안정 id의 중복은 기존 런타임 검증으로 차단한다.

**테스트**: 기존 lines 호환·JSONL 검증/원문 비노출·표시 정보·id 제외·배열 실제 argv를 포함해 최종
3057 passing / 3 pending.

## [0.7.36] - 2026-08-19

### 개선 — QuickPick 선택 기억 안정화

- QuickPick 객체 항목에 선택적 `id`를 지정할 수 있다. label을 번역·수정하거나 같은 label을 여러
  mapping에 써도 `rememberLastSelection`과 History 재실행이 현재의 정확한 항목을 찾아 새 label/value를
  다시 만든다. id가 없는 기존 액션은 종전처럼 고유 label로 복원되므로 호환된다.
- 한 QuickPick 안의 중복 item id는 런타임과 Doctor가 실행 전에 오류로 알린다. 다중 선택은 id가 있는
  항목과 없는 항목이 섞여도 History의 항목 경계를 보존하며, 저장된 id가 사라졌을 때 같은 label의
  다른 mapping으로 후퇴하지 않는다.
- Command Palette의 `TaskHub: QuickPick 선택 기억 초기화`로 현재 워크스페이스의 기억값을 한 번에
  지울 수 있다. 다중 선택이던 task를 단일 선택으로 바꾼 경우 과거 다중 기억은 설정 오류를 일으키지
  않고 무시되어 사용자가 현재 목록에서 다시 고를 수 있다.

**테스트**: 안정 id 기억·label 변경/중복 History 재파생·혼합 다중 선택·기억 초기화를 포함해 최종
3051 passing / 3 pending.

## [0.7.35] - 2026-08-19

### 개선 — QuickPick 실행값 참조 단순화

- QuickPick·inputBox·envPick처럼 `value`를 내는 태스크는 `${choice}`를 `${choice.value}`의
  축약으로 사용할 수 있다. 배열 mapping도 그대로 argv 여러 칸으로 펼쳐지고 `value: []`는 인자를
  만들지 않으므로, 선택지마다 `when` 태스크를 복제하지 않고 command 하나로 연결할 수 있다.
- value mapping이 있는 QuickPick의 `${choice.label}`을 command 인자로 쓰면 Doctor가 표시 문구와
  실행값의 차이를 info로 안내한다. label 자체가 필요한 액션은 기존 참조를 그대로 사용할 수 있다.

**테스트**: bare scalar/배열 argv·Preview 전방 참조·Doctor 보안 열거를 포함해 최종 3041 passing / 3 pending.

## [0.7.34] - 2026-08-19

### 추가 — 배열 결과를 태스크 반복 실행으로 바로 연결

- 비대화형 태스크에 `"forEach": "${files.paths}"` 또는 정적 문자열 배열을 지정하면 항목마다
  태스크를 순차 실행한다. 반복 안에서는 `${each}` / `${each.value}`, `${each.index}`(0부터),
  `${each.number}`(1부터), `${each.count}`를 사용할 수 있어 여러 파일을 처리하려고 태스크를
  복제하거나 별도 셸 반복문을 작성할 필요가 없다.
- 배열은 문자열로 합쳤다가 다시 나누지 않으므로 공백이 든 경로도 원래 경계를 유지한다. 동적
  목록은 1,000개로 제한하며, 실패하면 `2/3`처럼 위치를 밝히고 남은 반복을 실행하지 않는다.
- 반복 command/shell의 실제 명령은 History와 Action Run Report에 마지막 한 줄만이 아니라 실행
  순서대로 모두 남는다. password·환경변수·클립보드·선택 텍스트에서 파생된 반복값은 실제 프로세스에만
  전달하고 기록에는 `***`로 가린다. 명령 누적은 태스크당 256KB에서 생략 표식과 함께 제한한다.
- 환경변수·클립보드·선택 텍스트의 민감 표식은 중간 결과까지 전파하며 Preview·History·실행 보고서·
  QuickPick 기억과 출력 표면에서 원문을 가린다. 민감 파일 저장 알림과 권한 오류 로그는 경로의
  정적 부분만 남기고 민감 입력에서 파생된 부분을 `***`로 가린다.
- 반복 결과는 `${task.count}`, `${task.outputs}`, `${task.stderrs}`, `${task.paths}`를 제공한다. 문자열
  출력의 `${task.output}`·`${task.stderr}`는 각 반복 출력을 줄바꿈으로 합친 값이며, 기존 단일 결과
  키는 마지막 반복값을 유지한다. 누적 결과도 액션 전체 결과 상한을 적용해 메모리가 무제한 커지지 않는다.
- `forEach` 소스와 task-level `when.var`의 `${each.*}`는 반복 지역값이 아니라 동명 producer 참조로
  의존성을 추론한다. 반복 본문의 `${each.*}`만 지역값이며, Doctor의 중첩 인터프리터 검사도 이를
  고정 목록 task로 오인해 면제하지 않는다. timeout 오류에는 현재 반복 위치를 포함한다.

**테스트**: 다중 파일 실제 argv·중간 실패·History/실행 보고서 명령 누적·password 파생값 마스킹·
Preview·Doctor·자동완성을 포함해 최종 3038 passing / 3 pending.

## [0.7.33] - 2026-08-18

### 추가 — QuickPick 기본 선택·직접 입력·선택 기억

- `default`에 항목의 `label`을 적으면 QuickPick을 열 때 그 항목이 활성화된다. 다중 선택에서는
  label 배열을 미리 선택하며, 앞 태스크 결과도 `${suggested.output}`처럼 사용할 수 있다.
- 단일 선택에 `allowCustom: true`를 쓰면 목록을 검색하다가 없는 값을 직접 입력해 그대로 다음
  태스크에 전달할 수 있다. 직접 입력 항목은 UI에서 구분해 보여 주며 `canPickMany`와의 잘못된
  조합은 스키마와 런타임에서 명확히 거부한다.
- `rememberLastSelection: true`는 마지막 label과 직접 입력 여부를 워크스페이스·액션·태스크별로 기억해 다음 일반
  실행에서 복원한다. 명시한 `default`가 있으면 기억값보다 우선하고, 목록에서 사라진 기억값은
  조용히 무시한다. 환경변수·클립보드·선택 텍스트·password에서 파생된 선택은 기억하지 않는다.
  구형 평문 기억은 활성화 시 제거하고, 저장소는 최근 100개 scope와 scope key·label 합계 64K자로 제한한다.
- History 재실행은 `labelList`로 다중 선택 경계를 보존하고 현재의 동적 label/value 정의에서 값을
  다시 만든다. 삭제된 정적 항목을 custom 입력으로 되살리거나, 중복 label의 다른 mapping을 조용히
  고르는 대신 해당 항목만 다시 묻는다. 결과에 `labelList`와 `custom`이 추가됐다.
- Preview Run은 세 설정을 표시하고, Doctor는 직접 입력 가능한 값을 고정 목록으로 오인해 셸 안전
  진단을 면제하지 않는다. 기존 QuickPick은 종전의 가벼운 실행 경로를 그대로 사용한다.

**테스트**: 기본 선택·다중 기본값·직접 입력의 실제 argv 전달·선택 기억·스키마·Doctor 안전 진단을
포함해 최종 2987 passing / 3 pending.

## [0.7.32] - 2026-08-18

### 추가 — 현재 파일·선택 영역·환경을 바로 쓰는 문맥 변수

- 액션 앞에 `fileDialog`나 환경변수 조회 태스크를 붙이지 않아도 `${file}`, `${relativeFile}`,
  `${fileDirname}`, `${fileBasename}`, `${fileBasenameNoExtension}`, `${fileExtname}`로 실행을 시작한
  순간의 활성 파일을 명령에 넘길 수 있다. Multi-root에서는 `${fileWorkspaceFolder}`가 활성 파일이
  속한 루트를, `${relativeFile}`은 그 루트 기준 경로를 가리킨다.
- `${selectedText}`, `${lineNumber}`, `${columnNumber}`, `${clipboard}`, `${env:NAME}`도 지원한다.
  에디터 문맥은 액션 시작 시 한 번 고정하므로 중간에 터미널이나 대화상자로 포커스가 옮겨져도
  뒤 태스크의 값이 바뀌지 않는다. 파일이 필요한 참조에 활성 파일이 없거나 환경변수가 정의되지
  않았으면 위험한 리터럴을 명령에 넘기지 않고 이유를 밝히며 실패한다. `??` 대안은 계속 쓸 수 있다.
- 환경변수·클립보드·선택 텍스트는 실제 프로세스에는 원문으로 전달하지만, 그 결과를 사용한 중간
  태스크까지 민감 표식을 전파한다. History·입력 프로필·QuickPick 기억·verbose 로그·실행 보고서·
  터미널/에디터에는 원문을 남기지 않고, 파일 저장은 `allowSecretContent: true`를 요구한다. Doctor는
  실제 확장 호스트 환경값을 읽지 않고도 같은 정책을 검사한다.
- `${` 자동완성에 모든 문맥 변수가 나타나고, `${env:` 뒤에는 현재 프로세스의 환경변수 **이름만**
  제안한다. Preview Run과 Doctor도 같은 예약 이름과 해석 규칙을 사용한다.
- 기존 action의 task id가 `file` 같은 내장 이름과 같으면 그 task가 우선한다. `${file}`은 기존 task의
  대표 결과, `${file.path}`는 그 task의 속성이며, 동명 task가 없을 때만 활성 파일 내장값을 쓴다.
- 활성 파일이 워크스페이스 밖에 있으면 절대 경로만 제공하고 상대 경로·가짜 workspace root를 만들지
  않는다. 에디터 스냅샷은 비동기 클립보드 읽기보다 먼저 잡아 포커스 이동에도 시작 시점 값을 유지한다.

**테스트**: 현재 에디터 자동 스냅샷·실제 argv 전달·기록 마스킹·동명 task 호환을 포함해 최종
2970 passing / 3 pending.

## [0.7.31] - 2026-08-18

### 수정 — `command` 태스크에서 배열 값이 인자 한 칸으로 뭉치던 문제

- `command` 타입에서 토큰 전체가 배열 참조인 경우(`ins ${pick.paths}`) 항목마다 argv 한 칸이
  된다. 이전에는 배열이 공백으로 이어 붙은 뒤 통째로 인용돼 `"a.bin b.bin"`이라는 **인자 한
  칸**이 됐고, 여러 파일·폴더를 고른 사용자는 도구가 존재하지 않는 경로 하나를 받는 것을 봤다.
  `args`는 처음부터 항목별로 펼쳤으므로, 같은 참조가 자리에 따라 다르게 동작하던 것이 사라졌다.
  `shell` 타입은 종전대로 공백 결합이다 — 문자열을 셸에 그대로 넘기는 계약이기 때문이다.

### 추가 — `quickPick` 항목의 `value` 매핑

- `items`의 객체 항목에 `value`를 적으면 목록에 **보이는 문구**(`label`)와 명령에 **들어가는
  값**을 가를 수 있다. 적지 않으면 `label`이 그대로 값이므로 기존 액션의 동작은 그대로다.
- `value`에 배열을 적으면 `args` 원소와 `command` 토큰 자리에서 인자 여러 개로 펼쳐지고,
  빈 배열은 아무 인자도 만들지 않는다 — "이 선택지에서는 옵션을 붙이지 않는다"가 된다.
- 결과에 `label`(표시 문구)과 `valueList`(값 배열)가 추가됐고, `canPickMany`에는 `labels`가
  더해졌다. `values`의 의미는 그대로다.
- History 재실행은 `label`로 선택지 유효성을 확인하고, **값은 지금의 정의에서 다시 파생시킨다** —
  매핑을 추가·수정한 뒤의 재실행이 저장된 옛 값(대개 표시 문구)을 명령에 넘기지 않는다.
- Doctor는 **참조한 키에 맞는 문자열**을 본다. `${pick.value}`는 매핑 값, `${pick.label}`은 표시
  문구로 판정하므로, 얌전한 값을 매핑해 두고 label에 위험한 문구를 적어 셸 메타문자 면제를
  뚫는 경로가 닫힌다. 항목 `value` 안의 미해결 참조도 `label`과 같은 기준으로 잡는다.

### 변경 — 실행 보고서를 찾을 수 있게 한다

- History 액션 항목의 우클릭 메뉴에 **실행 보고서 보기**가 항상 있다. 이전에는 로그가 있는
  기록에만 메뉴가 붙어, 기능이 있다는 사실 자체를 알 수 없었다. 인라인 아이콘은 종전대로 로그가
  있는 기록에만 나타난다.
- 로그가 없으면 **왜 없는지 확인한 뒤** 안내한다. 설정이 꺼져 있으면 저장 위치(`.taskhub/logs/`,
  자동 `.gitignore`)와 캡처 출력에 자격 증명이 섞일 수 있다는 사실을 함께 알리고 **실행 로그
  켜기** 버튼을 준다. 이미 켜져 있으면 "켜기 전에 기록된 실행" 이라고 말하고 아무 일도 하지 않는
  버튼을 내밀지 않는다. 실행 중인 항목도 설정이 꺼져 있으면 오지 않을 보고서를 기다리지 않게 한다.
- 켜기는 **사용자 설정**에 쓴다. `.vscode/settings.json`에 쓰면 저장소를 clone한 모든 사람의 출력
  캡처가 함께 켜지기 때문이다.

**테스트**: 실제 프로세스가 받은 argv로 확인하는 통합 시나리오 9건(IT-160~168) — `command` 문자열의
배열 확장, `args`와의 동치, `shell`의 문자열 결합 계약, 다중 선택 `valueList`, 매핑을 바꾼 뒤의 재실행 —
과 Doctor의 키별 판정·미해결 참조 검사, manifest `when` 회귀 고정 포함, 최종 2953 passing / 3 pending.

## [0.7.30] - 2026-08-15

### 변경 — password 파생 값의 파일 저장은 태스크가 선언해야 한다

- `writeFile`·`appendFile`·`output.mode: 'file'`이 `password: true` 입력에서 파생된 값을 쓰려면
  태스크에 `allowSecretContent: true`가 있어야 한다. 없으면 실행을 거부하고 켜야 할 플래그를
  알려 준다. **기존 동작 변경** — 이전 버전에서 비밀 값을 파일로 쓰던 액션은 플래그를 추가해야
  한다. `${token.value}`를 `content`에 끼워 넣었다는 이유만으로 아무 표시 없이 비밀을 디스크에
  남길 수 있던 것이 바뀐 부분이며, 이제 의도가 `actions.json`에 남는다.
- 선언한 저장은 파일을 소유자 전용 권한(`0600`, Windows에서는 효과 없음)으로 만들고, 저장
  사실을 경로와 함께 알린다. 덮어쓰기에서도 기존 권한을 다시 조인다.
- `editor`·`terminal` 출력은 종전대로 전면 차단이다 — 사용자가 위치를 고르지 않은 암묵적
  영속화이기 때문이다. 파일은 사용자가 경로를 적은 경우라 선언을 요구하는 쪽을 택했다.
- 이 정책 오류만은 비밀 실패 가림 대상에서 제외한다. 메시지에 비밀이 없고, 가리면 고치는
  방법을 볼 수 없기 때문이다.
- Doctor가 실행 전에 잡는다. `secret.file-optin`(error)은 선언 없이 비밀을 파일로 쓰려는
  태스크를, `secret.allow-unused`(warning)는 아무것도 허용하지 않는 선언을 알린다. 오염 판정은
  런타임과 같은 스캐너를 써서 전이까지 따라간다 — 비밀을 거친 결과를 쓰는 태스크도 잡힌다.

**테스트**: 플래그 없는 `writeFile`·`output.mode:'file'` 거부와 파일 미생성, 선언 시 저장·`0600`
권한·고지 알림과 Doctor의 오염 전이·무효 선언 검증 포함, 최종 2929 passing / 3 pending.

## [0.7.29] - 2026-08-15

### 추가 — History 액션 실행 보고서

- History 액션 항목의 **실행 보고서 보기**에서 실행 결과·시각·소요 시간·실패 사유·실패한
  태스크 이름·심각도별 진단 개수와 TaskHub가 직접 확정한 파일 결과를 태스크 요약 표와 함께
  확인할 수 있다. 태스크별 명령·캡처 출력 상세는 성공한 실행에서는 접고 실패·중지된 실행에서는
  펼쳐서 연다. 시그널로 종료된 태스크는 종료 칸에 시그널 이름을 표시한다.
- 보고서 버튼은 저장된 실행 로그가 있는 기록에만 나타난다. 캡처 출력이 표시 상한을 넘으면
  오류가 있는 **뒷부분**을 남기고, 패널에서 찾기(Ctrl/Cmd+F)를 쓸 수 있다.
- 보고서는 실행 당시 로그 스냅샷을 사용하므로 액션의 이름이 바뀌거나 정의가 삭제돼도 열린다.
  보관 정책으로 회전된 경우, 워크스페이스 폴더가 열려 있지 않은 경우, History 항목과 로그가
  서로 다른 실행인 경우를 각각 구분해 안내한다.
- 사용자 중지·비밀번호 파생 실패처럼 TaskHub가 만든 사유는 로그에 코드로 저장하고 보고서를
  여는 시점의 UI 언어로 표시한다. 도구가 낸 오류 원문은 번역하지 않는다.
- History에는 큰 본문 대신 워크스페이스 URI와 상대 경로만 저장한다. 보고서를 열 때 경로 탈출,
  중간 심볼릭 링크, 비일반 파일, 8MB 초과와 손상·지원하지 않는 JSON을 다시 거부하며,
  웹뷰는 스크립트 없이 CSP와 HTML 이스케이프를 적용한다.

**테스트**: 보고서 요약·시그널 종료·절단 표시·지역화 사유·표 시맨틱, 로그 스키마 거부와
경로·symlink·크기 방어, 실패 경로의 진단 수집과 비밀 파생 파일 경로 마스킹 검증 포함,
최종 2922 passing / 3 pending.

## [0.7.28] - 2026-08-14

### 추가 — 액션 실행 로그 저장과 회전

- `taskhub.runLogs.enabled`를 켜면 액션·태스크 상태, 소요 시간, 마스킹된 명령과
  실패 사유, 종료 코드, 이미 캡처된 stdout/stderr를 워크스페이스의 `.taskhub/logs/`에
  구조화된 JSON `.log` 파일로 남긴다. 터미널 스트림·백그라운드 one-shot 출력은 실행
  방식을 바꾸어 가로채지 않고 수집할 수 없다는 상태만 기록한다.
- `password: true` 파생 명령·`cwd`는 마스킹하고 출력은 저장하지 않는다. 일반 출력의
  미식별 비밀·프로젝트 데이터 영속화 위험 때문에 기본은 비활성화하고, Command Palette에
  **TaskHub: 실행 로그 폴더 열기**를 추가했다.
- 개별 로그 8MB 상한을 넘으면 잘림 상태와 원래 크기를 명시하고, 만료 기간 → 개수 →
  워크스페이스 총 용량 순으로 오래된 파일을 회전한다. 고정 경로의 심볼릭 링크를 거부하고
  원자적으로 쓰며, 저장·회전 실패는 액션 결과를 바꾸지 않는다. 로그 전용
  `.gitignore`가 없으면 자동 생성하고 기존 파일은 덮어쓰지 않는다.

**테스트**: 마스킹 경계·상한·원자 쓰기·기간/개수/용량 회전·symlink 거부와
실제 파이프라인→디스크 비밀 문자열 제외 통합 검증 포함, 최종 2892 passing / 3 pending.

## [0.7.27] - 2026-08-14

### 추가 — 이름 있는 입력 프로필

- History의 저장 입력을 워크스페이스 전용 프로필로 이름 붙여 보관하고, 액션 우클릭 메뉴에서
  프로필로 실행하거나 이름을 바꾸고 삭제할 수 있다. 같은 액션의 이름은 대소문자와 무관하게
  중복을 막으며, 기존 이름으로 저장하면 확인 후 입력값을 교체한다.
- 실행 시 현재 `validatePattern`·고정 Quick Pick 선택지·다중 파일/폴더 형식을 다시 검사한다.
  바뀌거나 검증할 수 없는 task ID/type은 오래된 것으로 표시하고, 명시적 확인 뒤 검증된 값만
  사용하며 나머지는 다시 묻는다. 0.7.27 이전 History에는 type 서명이 없어 보수적으로 다시 묻는다.
- `password: true` 입력은 History와 프로필 양쪽에서 제외하고, 프로필당 128KB·워크스페이스당
  50개/총 2MB 상한으로 `workspaceState`가 무한히 커지지 않게 한다. 손상된 개별 항목은 후속
  관리에서도 원본을 보존하며, 지원하지 않는 저장소 형식은 덮어쓰지 않는다.

**테스트**: 프로필 저장·stale 판정·비밀번호 제외와 저장소→파이프라인 재실행 통합 검증 포함,
최종 2884 passing / 3 pending.

## [0.7.26] - 2026-08-14

### 추가 — Memory Map에서 ELF 원본 바이트 보기

- AXF/ELF Memory Map의 심볼·섹션 행에서 **바이트 보기**를 누르면 원본 파일을
  Hex Viewer로 열고 해당 파일 오프셋 범위를 바로 선택한다. `sh_offset`을 우선 사용하고
  섹션 정보가 없는 주소는 `PT_LOAD`의 `p_offset`으로 변환한다.
- BSS/NOBITS와 segment의 zero-fill 꼬리는 파일 바이트가 없다고 명확히 안내하며, 섹션·파일
  경계를 넘는 범위는 다른 위치로 추측해 열지 않는다. 대응 바이너리가 없는 ARM Linker
  Listing에는 진입점을 표시하지 않고, Memory Map을 연 뒤 ELF가 바뀌면 다시 열도록 안내한다.
- Memory Map의 100MB 한도 안이더라도 Hex Viewer의 50MB 입력 한도를 넘는 ELF는 기존 대용량
  파일 안내와 함께 열기를 거부한다.

**테스트**: ELF 주소→파일 오프셋·NOBITS 경계와 Memory Map→Hex 초기 선택 통합 검증 포함,
최종 2869 passing / 3 pending.

## [0.7.25] - 2026-08-14

### 추가 — 장시간 액션 완료 알림

- 기본 10초 이상 실행된 액션이 끝나면 성공·실패·중지 결과와 소요 시간을 상태 표시줄에
  잠시 보여 준다. 완료 시 VS Code 창에 포커스가 없으면 알림도 보내며, 750ms 안의 성공·중지
  알림은 결과별 개수 하나로 묶어 폭주를 막는다. 실패는 액션 이름과 원인을 담은 기존 상세
  오류 알림을 개별로 유지하고 상태 표시줄 요약에만 합친다.
- 임계값·포커스 정책·대상 결과를 `taskhub.backgroundCompletion.*` 설정으로 조절한다.
  `taskhub.showTaskStatus`는 기존처럼 실행 피드백 전체의 마스터 스위치이며, 액션 지정
  메시지나 민감 디버그 알림과 같은 결과를 중복 표시하지 않는다.

**테스트**: 장시간 완료 정책·묶음·실행 종료 통합 검증 포함, 최종 2856 passing / 3 pending.

## [0.7.24] - 2026-08-14

### 수정 — Windows 민감 one-shot의 `.cmd`·스크립트 실행

- `.cmd`·`.bat` shim이나 스크립트를 쓰는 Windows 민감 one-shot이 실제로 실행된다.
  `powershell.exe`를 분리(detached) 프로세스로 띄우면 스크립트를 실행하지 않고 즉시
  성공으로 끝나 백그라운드 작업이 조용히 사라지고 실패 알림도 뜨지 않았다. 이 경로는
  분리 플래그 없이 띄우되 확장 호스트를 붙잡지 않는 방식은 유지한다.

**테스트**: Windows 민감 `.cmd` one-shot 회귀 검증 활성화, 최종 2850 passing.

## [0.7.23] - 2026-08-14

### 수정 — 콘솔 없는 Windows 민감 one-shot의 PowerShell 경로

- `.cmd`·`.bat`처럼 PowerShell이 필요한 민감 one-shot은 콘솔 없는 detached 프로세스에서
  `[Console]::OutputEncoding`을 설정하지 않는다. Windows PowerShell 5.1이 명령 실행 전에
  중단될 수 있는 불필요한 콘솔 접근을 피한다. raw 명령의 `>`·`>>` UTF-8 파일 출력
  설정은 그대로 유지한다.

**테스트**: 콘솔 비의존 프리앰블 단위 검증 포함, 최종 2850 passing.

## [0.7.22] - 2026-08-14

### 수정 — Windows 민감 one-shot 시작 안정성

- 비밀번호 입력을 사용하는 Windows one-shot의 native 명령은 PATH에서 판정한 실제
  실행 파일을 argv로 직접 띄운다. 불필요한 PowerShell·`ProcessStartInfo` 2단 래퍼에서
  작업이 시작되지 않거나 종료 실패 알림이 전달되지 않던 문제를 고쳤다.

**테스트**: Windows native·PowerShell 실행과 민감 detached one-shot 회귀 검증 포함, 최종 2849 passing.

## [0.7.21] - 2026-08-13

### 보안 — Windows 실행 파일 경로 고정

- Windows에서 직접 실행할 명령은 PATH 판정에서 찾은 실제 `.exe`/`.com` 절대 경로를
  스트림·캡처·one-shot 실행까지 그대로 사용한다. 작업 폴더의 동명 파일 등 판정과 다른
  실행 파일이 선택되어, 비밀번호에서 유래한 인자가 의도하지 않은 프로그램에 전달될 수
  있던 경로를 닫았다. 판정 뒤 파일이 사라지거나 실행할 수 없게 되면 원래 이름을 셸에서
  다시 찾지 않고 시작 실패로 처리한다.
- `cwd`에만 있는 실행 파일을 bare 이름(예: `"flash.exe"`)으로 지정한 기존 액션은
  `command`를 `".\\flash.exe"`처럼 명시적인 상대 경로로 바꾸거나 실행 파일 디렉터리를
  PATH에 추가해야 한다. 명시적인 상대 경로는 태스크의 실제 `cwd`를 기준으로 해석한다.

**테스트**: Windows 실행 경로 고정과 detached one-shot 회귀 검증 포함, 최종 2849 passing.

## [0.7.20] - 2026-08-13

### 내부 — Windows 실행 경로 판정의 회귀 가드

- 사용자에게 보이는 동작 변화는 없다. `shell`이 아닌 Windows 명령의 실행 방식 판정이 주입된
  실행 파일 조회를 그대로 쓰도록 맞춰, 확장자 없는 명령의 PATH 해석이 어긋나면 테스트가 잡는다.

**테스트**: 최종 2846 passing.

## [0.7.19] - 2026-08-13

### 수정 — Windows 명령 인자와 PowerShell 출력 호환성

- Windows PowerShell 5.1이 `shell` 태스크의 명시적 `args` 속 큰따옴표를 제거해
  `node -e` 같은 명령을 훼손하던 문제를 고쳤다. 셸 문법이 없는 단일 실행 파일은 argv로
  실행하고, 연산자·파이프·리다이렉션은 기존처럼 셸에서 처리한다.
- 비밀번호를 쓰는 Windows one-shot은 실제 argv를 인코딩된 래퍼 안에 두면서도 큰따옴표와
  종료 코드를 보존해, 백그라운드 실행과 실패 알림이 Windows에서도 동작한다.
- `taskhub.pipeline.windowsPowerShellEncoding: "utf8"`이 콘솔 캡처뿐 아니라 `>`/`>>` 파일
  리다이렉션에도 적용된다. Windows PowerShell 5.1은 UTF-8 BOM을 붙이고 PowerShell 7은
  BOM 없이 기록하므로, 엄격한 바이트 형식을 요구하는 다음 단계는 이 차이를 고려해야 한다.

**테스트**: Windows argv·one-shot·PowerShell 리다이렉션 회귀 검증 추가, 최종 2845 passing.

## [0.7.18] - 2026-08-13

### 보안 — 가져온 액션의 실행 위험 사전 검토

- Import는 Doctor 진단 유무와 관계없이 파일을 쓰기 전에 항상 신뢰 확인을 표시한다. 가져올 액션과
  명령·argv·cwd·env·파일 작업을 요약하고, 원본 검토를 기본 동작으로 제공한다.
- Doctor의 셸 보간·동적/중첩 인터프리터·경로 진단은 보조 정보로 구분하며, 진단이 없어도 고정 악성
  명령의 안전을 보증하지 않는다고 명시한다. 닫기·취소는 워크스페이스를 변경하지 않는다.
- 손상된 기존 `actions.json`을 검토 중 수정했다면 최신 내용을 다시 검증한다. 유효해진 파일은 정상
  병합하고, 여전히 손상된 파일은 동의 시점의 최신 내용만 백업해 방금 저장한 수정을 잃지 않는다.
- README에 액션을 실행 가능한 설정으로 명시하고 Restricted Mode와 정적 분석의 한계를 안내한다.

**테스트**: Import 신뢰 확인 13 케이스 포함, 최종 2841 passing.

## [0.7.17] - 2026-08-12

### 수정 — ScriptBlock 주입과 조건부 출력 필드 정합성

- PowerShell의 ScriptBlock 매개변수와 위치 인자를 코드 자리로 판정하고, 중괄호를 포함한 값의 주입
  위험을 Doctor가 경고한다. 일반 데이터 인자는 과잉 경고하지 않는다.
- 출력 필드의 활성 조건을 런타임·의존성 그래프·Doctor·Preview에서 통일했다. 결과 전달이 꺼진
  출력과 file 모드가 아닌 filePath·overwrite는 무시하며, language는 보간하지 않는다.
- 실행 파일 참조와 대체 체인은 실제로 풀릴 수 있는 키만 확장한다. 예약 변수와 bare 참조의 우선순위,
  자기 참조, 실패하거나 skip될 수 있는 태스크도 런타임 규칙에 맞췄다.
- Preview는 출력·capture·diagnostics가 무시되는 이유를 명확히 표시한다.

**테스트**: 신규 16 케이스, 최종 2823 passing.

## [0.7.16] - 2026-08-10

### 수정 — 대체 참조 보간, 명령 주입 검사, 유효 필드 모델 재작성

- command 문자열의 대체 참조가 공백에서 분리되어 리터럴로 실행되던 문제를 고쳤다.
- Doctor의 중첩 인터프리터 검사를 값의 문자 집합이 아니라 POSIX·cmd·PowerShell의 실제 문법 자리로
  재작성했다. 동적 인터프리터, 스크립트 인자, 리다이렉션·명령 치환·옵션 주입을 보수적으로 잡고,
  고정 스크립트 뒤의 데이터 argv 같은 안전한 회피책은 허용한다.
- 런타임이 보간하는 필드만 의존성·skip·Doctor·Preview가 읽도록 공용 순회 규칙을 만들었다.
  비활성 OS 분기, 정적 대체 필드, 리터럴 조건 피연산자가 가짜 의존성과 오류를 만들지 않는다.
- 비밀 마스킹용 실행 기록을 실제 argv와 맞추고, Doctor는 소스별 실패를 격리한다.
- 큰 액션에서 나타나던 반복 복사·재귀·중첩 스캔을 선형 처리로 바꾸고, 긴 순환 진단을 접어
  확장 호스트 정지와 과도한 메시지를 막았다.

**테스트**: 신규 67 케이스, 최종 2796 passing.

## [0.7.15] - 2026-08-10

### 수정 — C++ mangled 심볼 판정과 후보 표시

- **C++ 매개변수 타입을 함수 이름으로 오인했다**: mangled 이름 전체에서 식별자를 찾던 탓에
  `_Z3foo6Widget`(`foo(Widget)`)이 `Widget` 검색에도 걸렸다. 이제 Itanium ABI 의 **이름부만** 읽어
  타입·템플릿 인자를 후보에서 제외한다 ([src/memoryMapViewer.ts](src/memoryMapViewer.ts)). 표준 축약,
  리터럴·표현식·인자 팩, 생성자/소멸자와 상속 생성자도 구조에 맞게 건너뛴다.
- **깨졌거나 모르는 ABI 토큰에서 가짜 이름을 만들었다**: 한 칸씩 밀며 재동기화해 ABI tag·벤더
  확장·깨진 길이 숫자를 식별자로 되살렸다. 이 최소 파서는 디맹글러가 아니므로, 모르는 문법은
  엉뚱한 심볼로 이동시키지 않고 **매칭하지 않는 쪽**으로 처리한다.

- **Listing 안내와 후보 경로를 바로잡았다**: ARM Listing 의 `func` 행을 직접 보고 심볼 존재 여부를
  판단한다. 후보 파일은 basename 대신 워크스페이스 상대 경로를 써, 같은 이름의 debug/release
  빌드와 멀티루트의 동명 파일을 구분한다.

**테스트**: 신규 12 케이스, 최종 2729 passing.

## [0.7.14] - 2026-08-10

### 추가 — 소스에서 Memory Map 으로 점프 (로드맵 §3a)

- **C/C++ 편집기에서 커서 아래 심볼을 맵에서 연다**: 우클릭 *이 심볼을 맵에서 보기*나 명령 팔레트로
  열려 있는 Memory Map 의 해당 행에 이동한다 ([src/memoryMapViewer.ts](src/memoryMapViewer.ts)). 어느
  바이너리인지는 별도 매핑 설정 대신 열려 있는 패널로 정한다.
- **C++·최적화 심볼도 찾는다**: Itanium ABI 의 `길이+이름` 성분을 따라 mangled 이름을 비교하고,
  `.constprop.0`·`.isra.0` 같은 알려진 clone 접미사만 제거한다. 단순 부분 문자열은 일치시키지 않는다.
- 후보가 여럿이면 주소·크기·타입·영역·섹션·파일을 보여 주고, 마지막으로 보던 맵을 먼저 제안한다.

- **표시 상한과 검색 범위를 분리했다**: QuickPick 은 5,000개로 제한해도 심볼 검색은 전체 행을 본다.
- stripped 바이너리나 심볼 없는 Listing 은 해당 맵을 지목해 설명하고, 열린 맵이 없으면 *Memory Map
  열기* 버튼을 제공한다. 선택 영역은 식별자 하나일 때만 심볼명으로 쓴다.

**테스트**: 신규 24 케이스, 최종 2717 passing.

## [0.7.13] - 2026-08-09

### 변경 — Memory Map: *Go to Symbol* 이 실제 심볼로 이동한다

- **Go to Symbol 이 실제 심볼로 이동한다**: 함수·전역변수 또는 오브젝트/섹션 행을 주소·크기·타입·
  영역과 함께 고를 수 있다 ([src/memoryMapViewer.ts](src/memoryMapViewer.ts)). 영역 이동은 별도 묶음으로
  남는다.
- **접힘·검색·가상 스크롤 때문에 이동이 실패했다**: 대상이 보이도록 필요한 상태만 풀고 해당 행을
  현재 검색 매치로 잡는다. 동명 영역은 이름과 순번으로 구분하며, 행을 끝내 찾지 못해도 해당 영역까지
  이동한다.

- **진입점과 목록을 다듬었다**: macOS 에 `cmd+shift+o` 를 추가하고, Memory Map 이 활성일 때 명령
  팔레트에도 노출한다. 목록은 화면에 그려지는 0 초과 항목 중 큰 것부터 최대 5,000개만 담되 전체
  개수를 함께 표시하고, 심볼과 섹션을 정확히 구분한다.
- **이동을 보고 계속 추적한다**: `role="status"` 로 이동을 알리고 행에 포커스를 준다. 강조는 충분한
  대비를 가지며, 가상 스크롤과 정렬 뒤에도 같은 행 객체를 따라간다.

**테스트**: 신규 39 케이스, 최종 2693 passing.

## [0.7.12] - 2026-08-09

### 수정 — 비밀 마스킹·멀티루트·자동완성·파서 경계

- **조건 사유에 비밀번호가 평문으로 남았다**: `when` 판정에는 실제 값을 쓰되 로그에 표시할 때는
  명령줄·cwd·출력과 같은 마스킹을 적용한다 ([src/extension.ts](src/extension.ts)).
- **멀티루트 액션과 실행 폴더의 승자가 달랐다**: 동명 id 를 병합할 때 선택한 액션과 그 액션의
  `${workspaceFolder}`·cwd 를 한 루프에서 함께 정해, 다른 루트의 명령과 폴더가 섞이지 않게 했다.
- **자동완성이 유효한 참조를 다른 값으로 바꿨다**: 후보별로 대체 범위를 계산하고, 커서 뒤 텍스트가
  실제 후보와 정확히 이어질 때만 덮는다 ([src/variableCompletions.ts](src/variableCompletions.ts)). 태스크
  id 후보는 기존 `.key` 를 보존할 수 있지만 전역 참조·결과 키 후보는 대안 끝까지 교체한다.

- **불완전한 참조를 안전하게 완성한다**: 닫는 `}` 가 없으면 확실한 종결자를 찾은 경우에만 커서 뒤를
  덮어 명령 인자를 삼키지 않는다. 커서가 `??` 연산자 안에 있으면 체인을 망가뜨릴 수 있어 제안하지
  않는다.
- **변조된 ELF 의 메모리 폭증을 막았다**: 심볼 엔트리 최소 크기·섹션 범위·심볼 개수 상한을 검증한다
  ([src/elfParser.ts](src/elfParser.ts)).
- **바이너리를 HEX/SREC 로 오인했다**: 알려진 확장자는 그대로 신뢰하고, 확장자로 알 수 없는 파일만
  길이·자릿수·체크섬이 유효한 레코드를 실제로 찾은 뒤 텍스트 포맷으로 연다
  ([src/hexParser.ts](src/hexParser.ts), [src/hexViewer.ts](src/hexViewer.ts)).
- **원시값 배열이 JSON Editor 를 멈췄다**: 객체가 아닌 행은 현지화된 값 열에 읽기 전용으로 표시하고,
  commit 과 행 추가도 같은 타입 규칙을 지킨다 ([src/jsonEditor.ts](src/jsonEditor.ts)). 루트 자체가
  `null`·숫자·문자열이면 빈 패널 대신 객체나 배열만 지원한다는 오류를 보여 준다.

- 워크스페이스 액션 병합을 `resolveWorkspaceActions` 로 분리해 액션과 폴더 매핑을 함께 검증할 수 있게
  했다. `referencePrefixAt` 문서 위치와 0.7.11의 신규 테스트 수도 바로잡았다.

**테스트**: 신규 62 케이스, 최종 2654 passing.

## [0.7.11] - 2026-08-08

### 수정 — 낱말 중간에서 자동완성을 받으면 꼬리가 남았다

`${ask.va|lue}` 중간에서 자동완성을 고르면 `${ask.valuelue}` 가 됐다. 삽입 범위는 커서까지, 대체
범위는 현재 `??` 대안의 끝까지만 주어 `editor.suggest.insertMode` 설정을 그대로 따른다
([src/variableCompletions.ts](src/variableCompletions.ts), [src/extension.ts](src/extension.ts)). 대체는
`}`·문자열 끝·다음 `??`·공백을 넘지 않아 닫히지 않은 참조 뒤의 명령 인자를 삼키지 않는다. 커서가
`${` 를 아직 지나지 않은 경우에는 제안하지 않는다.

**테스트**: 신규 12 케이스, 최종 2592 passing.

## [0.7.10] - 2026-08-08

### 수정 — 입력과 무관하게 굳는 `when` 조건

0.7.9 는 미해결 `when.var` 만 잡았다. 이제 `in: []` 와 참조 없는 상수 `var` 처럼 입력과 무관하게
결과가 정해지는 조건을 `detectFrozenCondition` 한곳에서 찾는다. 연산자가 없거나 정규식이 깨진 경우는
기존 진단과 중복하지 않는다.

- **런타임과 같은 조건을 본다**: 여러 연산자가 잘못 함께 있으면 런타임 우선순위상 실제 적용될
  하나만 판정하고, 참조 여부도 런타임의 보간 정규식으로 확인한다.
- **Doctor 와 Preview 를 맞췄다**: `when.literal-operand` 는 참조가 비교되지 않을 뿐 아니라 불필요한
  의존성과 skip 전파도 만든다고 설명한다. Preview 는 굳은 결과를 태스크와 요약 양쪽에 표시한다.

**테스트**: 신규 16 케이스, 최종 2580 passing.

## [0.7.9] - 2026-08-08

### 수정 — 조건부 태스크(`when`)에 참조 검사가 하나도 닿지 않았다

`when.var` 가 검사 대상에서 빠져, 없는 참조가 리터럴로 비교되며 분기가 조용히 한쪽으로 굳었다.
Preview Run 에는 조건 자체가 보이지 않았다.

- **조건 변수도 일반 참조처럼 검사한다**: 미해결·키 오타·미캡처·`??` 진단을 그대로 적용한다
  ([src/doctor.ts](src/doctor.ts)). `when.dead-branch` 는 미해결 값 때문에 항상 실행되거나 절대
  실행되지 않는 결과를 밝히고, `when.literal-operand` 는 보간되지 않는 비교 피연산자의 참조를 잡는다.
- **Preview 에 조건을 먼저 표시한다**: 실제 입력을 모르는 정상 조건은 결과를 단정하지 않고, 입력과
  무관하게 굳은 경우만 명시한다 ([src/previewRun.ts](src/previewRun.ts)).
- 전방 참조가 든 `??` 체인은 시뮬레이션 결과가 아니라 원본 참조 순서로 판정해 선언 순서에 따라
  진단이 달라지지 않게 했다.

**테스트**: 신규 21 케이스, 최종 2564 passing.

## [0.7.8] - 2026-08-08

### 수정 — `??` 진단이 사실과 반대되는 말을 했다

풀리는 대안이 있는 `${pick.value ?? pick2.nope}` 에도 `variable.unresolved` 가 붙어 "리터럴로
전달된다"고 잘못 안내했다.

- **대안별로 판정한다**: 없는 태스크·자기 참조·없는 결과 키·미캡처 출력을 각각 분석한다
  ([src/previewRun.ts](src/previewRun.ts)). 하나라도 풀리면 `variable.dead-alternative`, 전부 실패할
  때만 `variable.unresolved` 로 나누며, 한 체인의 여러 결함도 한꺼번에 설명한다
  ([src/doctor.ts](src/doctor.ts)). 전방 참조도 선언 순서와 무관하게 같은 규칙을 쓴다.
- **런타임 순서와 범위를 따른다**: 결과 객체로 풀리는 bare 대안은 체인을 끝내므로 뒤 대안을 시도한
  것으로 보지 않는다. 비활성 OS 분기와 `itemsFromCommand` 가 대신하는 정적 `items` 는 검사하지 않는다.
- Preview 요약은 죽은 대안과 실제 미해결 참조를 별도로 센다.

**테스트**: 신규 20 케이스, 최종 2543 passing. 판정 결과를 런타임 보간과 표로 대조한다.

## [0.7.7] - 2026-08-08

### 수정 — 자동완성이 `??` 체인의 앞 대안을 지웠다

`${pickFile.path ?? pickFolder.` 에서 자동완성이 앞 대안의 키를 제안하고, 선택하면 뒤 대안을 통째로
지웠다. `referencePrefixAt` 이 커서가 있는 대안만 돌려주도록 해 제안과 대체 범위를 맞췄다
([src/variableCompletions.ts](src/variableCompletions.ts)). 대안 앞 공백은 보존하고, 분리는 런타임과 같은
`split('??')` 규칙을 써 잘못된 `???` 입력에도 실행되지 않을 참조를 제안하지 않는다.

**테스트**: 신규 14 케이스, 최종 2523 passing.

## [0.7.6] - 2026-08-07

### 수정 — 전방 `??` 참조 오진

0.7.5의 수정이 아직 실행되지 않은 태스크를 가리키는 전방 참조에는 적용되지 않아, 병렬 분기의
`${bA.output ?? bB.output}` 이 계속 미해결로 오진됐다.

- 보간·의존성·Doctor·Preview 가 `parseReferenceAlternatives` 하나로 `??` 대안과 `head.key` 를 읽는다
  ([src/pipelineUtils.ts](src/pipelineUtils.ts), [src/previewRun.ts](src/previewRun.ts),
  [src/doctor.ts](src/doctor.ts)). 하나라도 풀리는 체인은 리터럴로 남지 않는다는 런타임 규칙도 공유한다.
- `args.array-joined` 는 대안을 따로 추측하지 않고 전방 결과까지 포함한 컨텍스트에서 표현식 전체를
  순서대로 해석한다.

**테스트**: 신규 24 케이스, 최종 2509 passing.

## [0.7.5] - 2026-08-07

### 수정 — Doctor 가 `??` 를 오탐했다

Doctor 가 `??` 체인을 첫 `.` 에서 잘못 잘라 모든 병합 참조를 `variable.unresolved` 로 오진했다.
대안을 각각의 참조로 읽고, 뒤쪽 대안의 키 오타와 미캡처 출력도 숨지 않게 모두 검사한다
([src/previewRun.ts](src/previewRun.ts)).

**테스트**: 신규 5 케이스, 최종 2485 passing.

## [0.7.4] - 2026-08-07

### 추가 — 조건부 태스크 (`when`)

앞선 태스크 결과로 파이프라인을 나누는 `when` 을 추가했다. `equals`·`notEquals`·`matches`·`in` 중
하나를 쓰며, 조건의 참조도 의존성으로 추론해 생산자가 끝난 뒤 평가한다
([src/schema.ts](src/schema.ts), [src/pipelineUtils.ts](src/pipelineUtils.ts)).

- 조건이 거짓인 태스크는 실패가 아닌 skip 으로 처리하고, 실행 상태를 알리기 전에 판정한다.
- 꺼진 태스크를 일반 참조하는 소비자도 함께 건너뛴다. 분기 중 살아남은 값을 하나의 소비자가 받게
  하려면 `??` 를 쓰며, 모든 대안이 꺼졌을 때만 소비자도 skip 된다.
- Doctor 는 연산자가 없거나 여러 개인 조건과 잘못된 `matches` 정규식을 진단한다
  ([src/doctor.ts](src/doctor.ts)).

**테스트**: 신규 19 케이스, 최종 2480 passing.

## [0.7.3] - 2026-08-07

### 추가 — `??`: 먼저 풀리는 참조

- `${a.x ?? b.y}` 는 왼쪽부터 처음 풀리는 값을 쓰며 셋 이상도 연결할 수 있다. 모두 실패하면 빈
  문자열이 아니라 원래 참조 리터럴을 남긴다 ([src/pipelineUtils.ts](src/pipelineUtils.ts)).
- 의존성 추론은 모든 대안의 생산자를 기다린다. 분기나 `continueOnError` 로 결과가 없는 태스크의
  대체값을 안전하게 받을 수 있다.
- 대안 주변 공백은 `??` 체인에서만 다듬고, 일반 참조의 기존 동작은 유지한다. 분리 규칙은
  `splitCoalesceAlternatives` 로 보간과 의존성 추론이 공유한다.

**테스트**: 신규 6 케이스, 최종 2461 passing.

## [0.7.2] - 2026-08-07

### 수정 — 변환 버튼의 일방통행, Enter 자동반복, 셀 접근명

- **배열↔문자열 변환을 왕복 가능하게 했다**: 빈 배열은 문자열로 바꾸지 않고, 비어 있지 않은 문자열은
  쉼표가 없어도 배열로 바꿀 수 있다 ([src/jsonEditor.ts](src/jsonEditor.ts)).
- 변환 뒤 반대 버튼으로 이동한 포커스가 Enter 반복 입력을 받아 타입을 계속 왕복시키지 않게 했다.
- 셀의 접근 가능한 이름을 명시해 내부 변환 버튼 라벨이 셀 이름으로 읽히지 않게 했다.

**테스트**: 신규 5 케이스, 최종 2455 passing. 의존성 변경과 취약점은 없었다.

## [0.7.1] - 2026-08-07

### 수정 — 변환 배지와 행 삭제 접근성

- **변환 배지의 보이는 크기만 줄였다**: 투명한 확장 영역으로 24×24 클릭 타깃은 유지하면서 표의
  행 높이와 글자 배치를 되돌렸다 ([src/jsonEditor.ts](src/jsonEditor.ts)).
- 행 삭제 뒤 포커스만 옮기던 데서 그치지 않고 스크린리더 상태 영역에도 삭제를 알린다.

**테스트**: 최종 2450 passing.

## [0.7.0] - 2026-08-06

### 수정 — JSON Editor 접근성: 대비 · 타깃 크기 · 편집을 벗어나는 길

- 삭제 버튼에 배경/전경용 색 토큰을 짝지어 다크 테마 대비를 약 2.5:1에서 5.49:1로 높였다
  ([src/jsonEditor.ts](src/jsonEditor.ts)).
- 작은 버튼과 변환 배지의 클릭 영역을 WCAG 2.2 기준인 24×24 이상으로 넓혔다.
- 키보드 포커스 링을 drag grip뿐 아니라 모든 버튼·셀·탭에 적용했다.

- 배열 항목도 셀 밖으로 포커스가 나가면 저장하되, 같은 셀 안의 입력·삭제·추가 버튼 사이 이동은
  편집을 유지한다. 저장 후에는 사용자가 이동한 위치로 포커스를 복원한다.
- 배열의 ✕·+ 버튼에서도 Escape 로 편집을 취소할 수 있다.
- 변환·행 삭제로 표를 다시 그린 뒤 같은 셀이나 인접 행으로 포커스를 돌린다. 행 인덱스는 값의
  truthiness가 아니라 범위로 검사해 값이 `0` 인 행도 삭제할 수 있다.

**테스트**: 신규 25 케이스, 최종 2449 passing.

## [0.6.75] - 2026-08-06

### 변경 — JSON Editor 웹뷰 로직 모듈화 완료

- draft 생성과 활성 편집 상태 계산까지 공용 번들로 옮겨, 웹뷰에는 DOM 연결 코드만 남겼다.
- 소스 복사본과 정규식 기반 동기화 검사를 제거해 host와 웹뷰가 같은 로직을 실행한다.

## [0.6.74] - 2026-08-06

### 추가 — JSON 셀 타입 변환

- 문자열과 숫자·boolean·null·배열을 의도적으로 바꾸는 버튼을 추가했다.
- 손실 가능 변환은 막고 결과 미리보기, Undo, 스크린리더 알림을 제공한다.

## [0.6.73] - 2026-08-06

### 변경 — JSON Editor 공용 로직 모듈화 2단계

- 셀 값 변환, 배열 변환, 시트 구성과 경로 해석 등 여섯 함수를 공용 번들로 옮겼다.
- commit과 draft 생성이 같은 문자열 보존·경로 검증 규칙을 쓰도록 중복 구현을 제거했다.

## [0.6.72] - 2026-08-05

### 변경 — JSON Editor 공용 로직 모듈화 1단계

- 웹뷰 순수 로직을 별도 esbuild 번들로 만들고 host 테스트가 같은 코드를 직접 사용하게 했다.
- 번들 로드 실패를 화면에 알리고 웹뷰의 로컬 리소스 범위를 dist로 제한했다.

## [0.6.71] - 2026-08-05

### 수정 — 배열 셀 편집과 키보드 흐름

- 배열 편집 중에도 툴바 Undo/Redo가 먼저 셀을 commit한 뒤 동작한다.
- 따옴표·역슬래시가 든 열에서도 항목 추가가 실패하지 않도록 CSS 셀렉터 조립을 없앴다.
- 항목 삭제·추가 뒤 편집 상태와 포커스를 복원하고 변경 개수를 스크린리더에 알린다.

## [0.6.70] - 2026-08-05

### 수정 — JSON Editor 세션 검증

- 세션 id가 양의 정수인지 검사해 NaN·음수·소수로 만들어진 웹뷰가 모든 메시지를 버리는 일을 막았다.
- 가드의 실제 목적과 주석도 살아 있는 세션 계약에 맞췄다.

## [0.6.69] - 2026-08-05

### 수정 — 웹뷰 세션과 배열 편집 계약 보강

- 명시적으로 넘긴 0 세션도 거부하고, 더는 사용할 수 없는 기본 인자를 제거했다.
- 배열 동기화 실패의 null 계약을 삭제·추가 핸들러가 실제로 지키도록 실행 기반 테스트를 보강했다.

## [0.6.68] - 2026-08-05

### 수정 — 배열 셀 동기화 경계 검사

- 활성 시트가 없거나 행 인덱스가 오래된 경우 배열 셀 동기화가 웹뷰를 중단시키지 않고 빠져나온다.

## [0.6.67] - 2026-08-05

### 변경 — JSON Editor 세션 id 필수화

- 메시지를 절대 주고받을 수 없는 기본 세션 0을 없애고 모든 웹뷰 생성자가 실제 세션 id를 넘기게 했다.

## [0.6.66] - 2026-08-05

### 수정 — 재로드와 진행 중 저장의 경합

- 재로드나 외부 변경으로 baseline이 바뀌면 이전 저장 스냅샷을 폐기해, 늦은 저장 응답이 새 화면을
  clean으로 만들거나 baseline을 되돌리지 못하게 했다.

## [0.6.65] - 2026-08-05

### 정리 — 매크로 확장 단계 설명 교정

- 화면에 표시되지 않는 확장 단계·캐시 요약을 문서가 약속하던 내용을 바로잡고 죽은 요약 코드를
  제거했다. 확장 예산과 무한 재귀 방어는 유지한다.

## [0.6.64] - 2026-08-05

### 수정 — ZIP의 extensionPath 보간

- ZIP 태스크의 archive·source·cwd도 다른 태스크와 같은 컨텍스트를 써 extensionPath 변수가
  리터럴로 남지 않게 했다.

## [0.6.63] - 2026-08-05

### 수정 — Preview의 ZIP source 검사

- 런타임이 보간하는 ZIP source 배열을 Preview에도 표시하고 미해결 참조 검사에 포함했다.

## [0.6.62] - 2026-08-05

### 수정 — Preview 실행 실패 요약

- 참조가 모두 풀려도 플랫폼 도구 누락이나 워크스페이스 밖 경로 때문에 실패할 태스크를 최종 요약에
  별도로 표시한다.

## [0.6.61] - 2026-08-05

### 수정 — 참조 자동완성 설명 지역화

- 순수 모듈은 항목 종류만 반환하고 VS Code 경계에서 영어·한국어 설명을 만들어 자동완성 detail을
  현재 언어로 표시한다.

## [0.6.60] - 2026-08-05

### 수정 — 다중 quickPick 결과 자동완성

- canPickMany가 켜진 quickPick에서 실제 결과 키인 values를 제안하고 단일 선택에서는 숨긴다.

## [0.6.59] - 2026-08-05

### 수정 — 저장 상태·배열 타입·실패 알림 정합성

- 저장이 겹치거나 먼저 실패해도 남은 pending 저장을 기준으로 dirty 상태를 계산한다.
- 패널 dispose 시 세션을 무효화하고 예상 밖 저장 실패도 사용자에게 알린다.
- 숫자 배열에 추가한 값이 혼자 문자열이 되지 않도록 빈 배열 항목의 타입 추론을 바로잡았다.
- 매크로 문서와 주석을 실제 표시 동작에 맞추고, 배포 스크립트의 commit·동기화 경로를 직접 시험한다.

## [0.6.58] - 2026-08-04

### 수정 — 데이터 손실·무한 처리·진단 불일치 종합 수정

- JSON Editor가 셀을 열었다 닫는 것만으로 배열 타입을 문자열로 바꾸거나, 진행 중 편집을 저장 완료로
  처리하거나, 이전 파일의 저장 결과를 새 세션에 적용하던 문제를 고쳤다.
- 미커밋 입력을 복구 스냅샷에 포함하고, capture 실패 시 stdout 전체가 잘못 들어가지 않게 했다.
- 매크로 확장에 깊이·단계·출력 예산과 memoization을 적용해 순환·중복 확장이 host를 멈추지 않게 했다.
- 예약 키·capture 이름·ZIP tool 보간을 런타임 계약에 맞췄다.
- Preview와 Doctor가 전방 참조, 다중 선택 결과, 플랫폼별 tool, 공백 참조를 런타임과 같은 규칙으로
  검사하도록 정리했다.

## [0.6.57] - 2026-08-03

### 추가 — 다중 파일·폴더 선택과 참조 자동완성

- fileDialog와 folderDialog가 여러 경로를 선택해 배열 결과로 전달할 수 있다.
- actions.json에서 태스크 id와 실제 결과 키를 참조 자동완성으로 제공한다.
- 배열 참조가 문자열 인자에 잘못 합쳐지는 경우를 Doctor가 경고하고, 이전 History 재실행의 결과
  형태도 호환되게 보정한다.

## [0.6.56] - 2026-08-02

### 수정 — Memory Map 검색과 영역 일괄 토글

- 검색이 Object Summary에도 동일하게 적용되며 숨은 항목 수를 정확히 표시한다.
- 일부 영역만 펼쳐진 상태에서 일괄 토글이 사용자의 기대와 반대로 움직이지 않게 했다.

## [0.6.55] - 2026-08-01

### 수정 — Memory Map Object Summary 접근성

- 섹션 행 토글을 접힌 영역 밖으로 옮기고 실제 상태와 aria-expanded를 동기화했다.
- 빈 영역과 사용량 막대도 일관되게 열리며 키보드 포커스, 제목 구조, 스크린리더 이름을 보강했다.
- 펼침·접힘 불변식과 웹뷰 스크립트 문법을 자동 검사한다.

## [0.6.54] - 2026-07-31

### 변경 — 다이얼로그 시작 위치 연속성

- 액션별 기억 위치가 없으면 같은 종류의 직전 다이얼로그 위치를 이어받되, 명시한 defaultUri가 항상
  우선한다.

## [0.6.53] - 2026-07-31

### 수정 — 웹뷰 메시지 계약 검사

- 발신자가 없는 Hex Viewer copySelection 처리기를 제거했다.
- 등록된 웹뷰의 송수신 메시지가 서로 대응하는지 검사해 죽은 핸들러와 미처리 발신을 막는다.

## [0.6.52] - 2026-07-31

### 수정 — Preview·Doctor·취소·아카이브 동작 통합 정리

- 다중 선택 결과와 미캡처 출력에 대한 Doctor 오진을 바로잡았다.
- 내장 ZIP/Unzip 상대 경로를 워크스페이스 기준으로 해석하고 Preview도 실제 착지점을 보여 준다.
- 프롬프트 취소를 실패나 사용자 중지와 구분해 History와 알림에 일관되게 기록한다.
- 마법사의 마지막 저장 단계, Hex 메시지 타입 검사, 한영 문구 대칭과 관련 문서를 함께 정리했다.

## [0.6.51] - 2026-07-30

### 추가 — fileDialog 다중 선택과 argv 배열 확장

- fileDialog가 여러 파일을 paths 배열로 반환한다.
- command의 args 원소가 배열 참조 하나일 때 여러 argv로 펼쳐 공백이 든 경로의 인자 경계를 보존한다.
- 빈 배열·단일 값·다른 문자열과 Preview도 같은 규칙을 따른다.

## [0.6.50] - 2026-07-30

### 수정 — 명령 실행·복구·Windows·웹뷰 후속 보강

- command 보간값이 argv 경계를 새로 만들지 못하게 하고, 중첩 셸·인터프리터로 다시 파싱되는 값은
  Doctor가 별도로 경고한다.
- JSON 복구 fallback이 신선도 불일치 때문에 무력화되던 문제를 고쳐 스냅샷을 보존한다.
- Hex Viewer의 이전 세대 타이머가 새 파일을 덮지 않도록 dispose와 세대 번호에 묶었다.
- Links와 Favorites를 편집하지 않았을 때 유효·무효 항목의 원본 값을 모두 보존한다.
- Windows PowerShell 실행 파일의 실제 경로와 종료 코드를 유지하고, Preview 표시 argv를 런타임과
  맞췄다.
- 배포 액션과 문서 예제를 Doctor로 직접 검사해 raw shell 주입 패턴이 다시 들어오지 않게 했다.

## [0.6.49] - 2026-07-30

### 수정 — 생성 액션 주입, 설정 보존, Hex 초기화, Windows 셸 계약

- 마법사·번들·예제의 동적 값을 raw shell 대신 command argv로 전달하고, 사용자 정의 raw shell
  보간은 Doctor가 경고한다.
- Links와 Favorites의 스키마 불일치 항목을 읽기에서 제외하더라도 다음 저장 때 원본 그대로 보존한다.
- Hex Viewer와 Custom Editor가 공용 ready 핸드셰이크를 쓰고 인스턴스별 상태를 유지한다.
- Windows raw shell의 스트림·캡처·one-shot 모드를 같은 PowerShell 규칙으로 통일했다. 인용 안의
  연산자는 오진하지 않고, 필요한 버전·인코딩·프로세스 그룹 처리를 보장한다.

## [0.6.48] - 2026-07-30

### 수정 — 비밀 파생값·복구 확인·프로세스 추적

- password에서 파생된 출력도 editor·terminal·hot-exit 저장 대상으로 나가지 않게 표시 전에 막는다.
- 테스트 관찰 배열이 프로덕션 메모리에 쌓이던 seam을 제거했다.
- JSON 복구 알림은 Esc로 닫았을 때 데이터를 버리지 않고 명시적 폐기 선택에만 삭제한다.
- 자식 프로세스 error 이벤트가 실제 생존 프로세스의 종료 추적을 성급히 해제하지 않게 했다.

## [0.6.47] - 2026-07-29

### 수정 — Hex 초기 데이터 전달과 shell·command 계약 분리

- Hex 웹뷰가 ready를 보낸 뒤 데이터를 전달하고, 핸드셰이크 실패 시 제한된 fallback을 사용한다.
- Hex·JSON 실제 진입점을 구동하는 테스트 seam을 추가하고 복구 저장소의 테스트 격리를 보강했다.
- shell은 운영체제 셸 문법을 실행하고 command는 실행 파일과 argv를 직접 spawn하도록 이름과 실제
  동작을 일치시켰다. 기존 shell 액션의 연산자·리다이렉션도 다시 동작한다.

## [0.6.46] - 2026-07-28

### 수정 — 아카이브·취소·비밀·상태 관리 종합 보강

- 출력 총량 초과나 사용자 중지 시 병렬 형제와 실제 프로세스 트리를 제한 시간 안에 종료하고,
  세대별 실행 상태를 분리해 재실행 간 간섭을 막았다.
- password 입력과 파생값을 명령·cwd·출력·History·terminal·editor에서 마스킹하며, 민감 디버그는
  명시적으로 다시 실행할 때만 제한적으로 허용한다.
- ZIP/Unzip에 AbortSignal을 연결하고 엔트리 수·크기·총량 상한, 손상 스트림 정리, 원자적 생성·교체를
  적용했다.
- 심볼릭 링크·하드 링크·검사 후 교체 경합을 통해 압축 대상이나 추출 경계를 벗어나는 경로를 막았다.
- Memory Map HTML은 직렬화 전에 크기를 검사하고, JSON 복구 저장소 초기화와 진행 중 저장의 경합을
  차단했다.
- 마법사 Back과 입력 복원, 중지와 실패의 History 구분, 깨진 Links/Favorites 안내, JSON Editor
  키보드 변환을 추가했다.
- 실제로 동작하지 않던 리다이렉션 안내와 번들 예제를 shell·command 계약에 맞게 수정했다.

## [0.6.45] - 2026-07-28

### 수정 — 남은 메모리 폭증 경로 제한

- ZIP 해제를 yauzl 스트리밍으로 바꿔 압축 데이터를 통째로 메모리에 올리지 않는다.
- Memory Map HTML 저장에 크기 상한을 두고 불필요한 문자열 복사를 줄였다.
- PTY가 열린 전후의 출력 버퍼에 모두 상한을 적용한다.

## [0.6.44] - 2026-07-28

### 수정 — Memory Map 오브젝트 이름 정렬

- 이름 열은 문자열로, 주소·크기·백분율처럼 실제 숫자 열만 수치로 비교한다.

## [0.6.43] - 2026-07-28

### 추가 — 파이프라인 결과 총량 상한

- 전체 태스크 결과 합계에 taskhub.pipeline.totalOutputLimitMb 설정을 추가했다.
- 기본 32MB이며 개별 태스크 상한보다 작아지지 않도록 실효 한도를 계산한다.

## [0.6.42] - 2026-07-28

### 수정 — Hex Viewer 데이터 복제 제거

- 바이트를 Base64로 HTML에 인라인하던 구조를 Uint8Array postMessage 전달로 바꿔 초기 메모리 복제를
  크게 줄였다.

## [0.6.41] - 2026-07-28

### 정리 — Hex 파서 엔트리 상한 현실화

- 도달할 수 없던 1억 개 상한을 입력 크기와 실제 위험에 맞는 3,200만 개로 낮췄다.

## [0.6.40] - 2026-07-28

### 수정 — ARM Listing 엔트리 상한

- 파일 크기와 별개로 파싱 엔트리를 50만 개로 제한하고, 잘린 사실을 알리면서 전체 기준 요약은
  유지한다.

## [0.6.39] - 2026-07-28

### 수정 — ZIP 해제 크기 상한

- 내장 해제 엔진에 엔트리당 512MB, 전체 2GB 제한을 적용해 zip bomb의 메모리·디스크 고갈을 막았다.

## [0.6.38] - 2026-07-28

### 수정 — 테스트 산출물 정리

- compile-tests 전에 기존 출력 디렉터리를 비워 삭제된 테스트가 남아 거짓 성공을 만들지 않게 했다.

## [0.6.37] - 2026-07-28

### 수정 — 중지·정렬·출력 상한 후속 보강

- continueOnError나 대기열 때문에 사용자 중지가 무시되지 않게 하고 셸 프로세스 트리까지 종료한다.
- envPick probe도 취소 가능하게 만들고 출력 상한을 스트림 단계에서 즉시 적용한다.
- Memory Map 숫자 정렬과 Hex 마지막 unit 이동 경계를 바로잡았다.
- JSON 복구 스냅샷과 envPick 출력에도 메모리 상한을 적용하고 다이얼로그 설정 설명을 교정했다.

## [0.6.36] - 2026-07-28

### 수정 — JSON 복구 저장소 상한

- dirty-close 스냅샷을 최신 20개·총 32MB로 제한하고, 저장뿐 아니라 기존 상태를 불러올 때도 오래된
  항목부터 정리한다.

## [0.6.35] - 2026-07-27

### 수정 — 취소·키보드·정렬 후속 보강

- envPick·confirm·itemsFromCommand 준비 중에도 중지가 실제 작업과 자식 프로세스를 취소한다.
- JSON Editor 탭 포커스와 Memory Map aria-expanded를 모든 상태 변경 경로에서 유지한다.
- Hex Viewer 키보드 선택이 마지막 불완전 unit 밖으로 나가지 않게 했다.
- 위치 기억을 끈 저장 대화상자와 아주 작은 백분율 정렬을 실제 동작에 맞췄다.

## [0.6.34] - 2026-07-27

### 수정 — Memory Map Object Summary 정렬

- 오브젝트와 소속 섹션 행을 함께 이동하고, 행의 원본 수치 속성으로 주소·크기·백분율을 정렬한다.

## [0.6.33] - 2026-07-27

### 수정 — 다이얼로그 위치 저장소 상한

- 사용하지 않는 scope가 전역 상태에 무한히 쌓이지 않도록 단일 맵과 LRU 상한을 적용하고 옛 키를
  마이그레이션한다.

## [0.6.32] - 2026-07-27

### 수정 — 액션 마법사와 트리의 id 출처 통일

- 마법사도 워크스페이스·프리셋·번들 액션을 트리와 같은 resolver로 읽어 중복 id를 정확히 피한다.
- 관련 죽은 예외 처리와 사용하지 않는 인자를 제거했다.

## [0.6.31] - 2026-07-27

### 수정 — 웹뷰 키보드 접근성과 마법사 경계

- Memory Map 영역·정렬, JSON Editor 셀 편집, Hex 바이트 선택을 키보드로 사용할 수 있게 했다.
- JSON 탭 패턴과 ARIA 상태를 완성했다.
- 생성될 command id를 확인 단계에 표시하고 원격 파일시스템 defaultUri를 보존한다.

## [0.6.30] - 2026-07-27

### 수정 — 위치 기억 비활성화 계약

- taskhub.dialog.rememberLastLocation을 끄면 확장이 defaultUri를 강제로 넣지 않고 VS Code의 최근
  경로 동작에 맡긴다. 액션이 명시한 defaultUri는 그대로 존중한다.

## [0.6.29] - 2026-07-27

### 수정 — 대화형 태스크 중지

- quickPick·inputBox·confirm 대기 중에도 중지할 수 있도록 실행별 취소 스코프를 연결했다.
- 운영체제 파일 대화상자는 강제 종료할 수 없음을 UI에서 구분하고, 이미 끝난 대상은 사실대로 알린다.

## [0.6.28] - 2026-07-27

### 변경 — manifest와 웹뷰 지역화

- 명령·설정·메뉴 설명과 웹뷰의 남은 문자열을 한영 번들로 옮겼다.
- Memory Map 속성에 들어가는 지역화 문자열도 escape한다.

## [0.6.27] - 2026-07-27

### 수정 — 액션 마법사 저장·검토·메뉴 흐름

- 확인 단계에서 id를 바꾼 뒤 바로 실행해도 새 command가 등록되도록 했다.
- 자세히 보기 문서를 여는 동안 modal이 작업을 막지 않게 하고 저장 단계로 돌아올 수 있게 했다.
- 실행 후 상태가 붙은 액션에도 편집·내보내기·삭제 메뉴가 계속 보인다.
- 로케일과 문자열 탐지 테스트가 실제 양쪽 언어를 검사하도록 바로잡았다.

## [0.6.26] - 2026-07-27

### 수정 — 웹뷰 지역화와 설정 설명

- 번들 누락으로 영어에 남은 문자열과 정렬 해제 안내를 지역화했다.
- showTaskStatus가 아이콘·설명만 숨긴다는 실제 범위를 문서화하고 하드코딩 문자열 검사를 추가했다.

## [0.6.25] - 2026-07-27

### 추가 — 유니코드 액션 id와 저장 전 편집

- 한글·악센트 제목도 의미 있는 id로 변환하고 command id 충돌을 피한다.
- 저장 전 확인 단계에서 id를 직접 수정하고 추가될 액션 JSON을 자세히 볼 수 있다.

## [0.6.24] - 2026-07-27

### 변경 — 빈 상태와 번들 예제 분리

- builtinActions가 auto이면 번들 예제를 실제 액션 목록에 넣지 않고 빈 상태 CTA로만 안내한다.
- 로드 실패 행은 오류가 난 정확한 설정 파일을 연다.

## [0.6.23] - 2026-07-27

### 수정 — 액션별 다이얼로그 위치

- 같은 task id를 쓰는 서로 다른 액션이 마지막 파일·폴더 위치를 공유하지 않도록 기억 scope에
  action id를 포함했다.

## [0.6.22] - 2026-07-27

### 수정 — 일괄 중지 경합

- 중지 요청 플래그를 실행 종료까지 유지해 늦은 완료가 History를 다시 실행 중으로 만들지 않게 했다.
- 중지 확인을 취소하면 터미널을 닫거나 상태를 바꾸지 않는다.

## [0.6.21] - 2026-07-26

### 변경 — Memory Map 지역화와 접근성

- 웹뷰 문자열을 VS Code 언어에 맞추고, 표 정렬·영역 펼침을 키보드로 조작할 수 있게 했다.
- aria-sort·aria-expanded·live region·레이블을 실제 상태와 동기화했다.

## [0.6.20] - 2026-07-26

### 변경 — Hex Viewer 지역화와 접근성

- 웹뷰 문자열을 지역화하고 폼 레이블, 아이콘 버튼 이름, live region, 표 구조를 보강했다.

## [0.6.19] - 2026-07-26

### 변경 — JSON Editor 지역화와 접근성

- 웹뷰 전체 문자열을 지역화했다.
- 키보드로 행 순서를 바꾸고 아이콘 버튼의 이름과 변경 알림을 스크린리더에 제공한다.

## [0.6.18] - 2026-07-26

### 추가 — 액션 생성 전 검토

- 마법사가 저장 전 최종 JSON과 Doctor 진단 차이를 modal로 보여 주고 저장·바로 실행·수정·취소를
  선택하게 했다.

## [0.6.17] - 2026-07-26

### 추가 — 액션 생성 템플릿 확장

- 마법사 템플릿을 2종에서 6종으로 늘려 명령 실행, 파일 선택, 입력, 확인, 다단계 워크플로를 만든다.
- 태스크 조립을 순수 함수로 분리해 템플릿 결과를 직접 검증한다.

## [0.6.16] - 2026-07-26

### 수정 — 태스크 상태 표시 설정 유지

- showTaskStatus를 끈 상태가 트리 재렌더와 설정 변경 뒤에도 즉시 유지된다.
- 표시만 숨기고 contextValue의 실제 실행 상태와 중지 기능은 보존한다.

## [0.6.15] - 2026-07-26

### 변경 — 액션 빈 상태와 제목 표시줄 정리

- 액션 없음·로드 실패를 구분하는 viewsWelcome과 복구 진입점을 추가했다.
- 버전 행을 제목으로 옮기고 제목 표시줄 아이콘을 자주 쓰는 세 개로 줄였다.

## [0.6.14] - 2026-07-26

### 변경 — 번들 예제 액션 표시 정책

- builtinActions 설정을 auto·always·never로 나눠 예제 액션을 프로젝트 액션과 구분한다.
- 액션 소스와 병합 우선순위를 문서화했다.

## [0.6.13] - 2026-07-26

### 변경 — 실행 중지와 터미널 닫기 분리

- 실행 중인 액션만 중지 대상으로 표시하고 중지와 터미널 닫기를 별도 명령으로 나눴다.
- 대상과 개수를 확인한 뒤 중지하며 History 상태와 완료 아이콘을 정확히 유지한다.

## [0.6.12] - 2026-07-26

### 변경 — Run Any Action 최근 목록 정합성

- 별도 저장 목록 대신 실제 History에서 최근 액션을 중복 없이 유도해 팔레트와 실행 기록을 맞췄다.

## [0.6.11] - 2026-07-26

### 추가 — 파일·폴더 다이얼로그 위치 기억

- 액션·태스크·대화상자 종류별 마지막 위치를 기억하며 설정으로 끌 수 있다.
- options.defaultUri를 실제로 적용하고 folderDialog가 입력 설정을 변형하지 않게 했다.
- adm-zip을 0.6.0으로 갱신했다.

## [0.6.10] - 2026-06-27

### 추가 — History 명령 보기와 저장 입력 재사용

- 실행한 명령을 History에서 열어 보고, 기본 재실행이 저장된 대화형 입력을 다시 사용한다.
- 런타임 의존성의 Node 엔진 요구사항을 확장 지원 범위와 맞췄다.

## [0.6.9] - 2026-06-10

### 수정 — 워크스페이스 경계 검사 정합성

- Preview와 Doctor도 realpath 기준 심볼릭 링크 탈출, null byte, 빈 경로를 런타임과 똑같이 거부한다.

## [0.6.8] - 2026-06-10

### 수정 — 워크스페이스 격리와 Hex 성능

- 쓰기·압축·출력 경로의 심볼릭 링크 탈출을 realpath 검사로 막았다.
- Hex 검색에 debounce와 매치 상한을 두고 호버 파이프라인 순서와 캐시를 개선했다.

## [0.6.7] - 2026-06-10

### 수정 — Hex 패널 격리와 숫자 호버 경계

- 여러 Hex Viewer의 메시지 핸들러가 서로의 패널을 갱신하지 않게 인스턴스별로 격리했다.
- 숫자 호버가 식별자 일부나 잘못된 경계를 숫자로 잡지 않게 했다.

## [0.6.6] - 2026-06-10

### 수정 — 64비트 값 표시 정확성

- Hex Viewer 8바이트 unit과 숫자 호버가 Number 정밀도 손실 없이 64비트 값을 표시한다.

## [0.6.5] - 2026-06-10

### 수정 — 미캡처 출력 참조 진단

- Doctor와 Preview가 결과 전달을 켜지 않은 태스크의 output 참조를 실제 런타임 실패로 진단한다.

## [0.6.4] - 2026-06-10

### 수정 — 파서의 조용한 오계산

- Intel HEX 확장 주소의 부호 오버플로, 32비트 이상 레지스터 필드, struct 멤버 누락을 바로잡았다.

## [0.6.3] - 2026-06-10

### 보안 — terminal 출력 모드의 명령 실행 제거

- 캡처한 출력 본문을 셸 명령으로 다시 실행하지 않고 터미널에 데이터로만 표시한다.

## [0.6.2] - 2026-06-10

### 수정 — JSON Editor 유니코드 손상

- Base64 디코딩을 UTF-8 안전 경로로 바꿔 한글·이모지 등 비ASCII 데이터의 저장 round-trip을 보장한다.

## [0.6.1] - 2026-06-03

### 추가 — 동적 quickPick과 inputBox 검증

- quickPick이 명령 출력으로 항목을 만들고 제외 패턴을 적용할 수 있다.
- inputBox에 validatePattern·validateMessage·extractPattern을 추가했다.
- Preview·Doctor·의존성 그래프가 동적 항목과 비활성 정적 items를 런타임과 같은 방식으로 처리한다.

## [0.6.0] - 2026-05-30

### 수정 — 파서·호버·JSON·진단 전반의 정확성 보강

- 레지스터 32비트 필드, struct·union·bitfield 크기, JSON 큰 숫자 보존을 바로잡았다.
- 비트 시프트, HEX/SREC·ELF·scatter 파싱, 매크로와 SFR 스캔의 경계 검사를 강화했다.
- 출력 capture 정규식과 Preview·Doctor 결과를 런타임에 맞추고 HTML 저장 실패를 사용자에게 알린다.
- Hex gap 표시, 링크 삭제, 액션 로드 오류, 명령 이름과 지역화·자원 정리를 함께 개선했다.

## [0.5.2] - 2026-05-28

### 변경 — 워크스페이스 Links 패널로 통합

- Built-in Links 패널과 번들 links.json을 제거하고 워크스페이스의 .vscode/links.json 하나만 사용한다.
- 확장 설치 폴더에 직접 추가한 링크는 자동 이전되지 않으므로 워크스페이스 파일로 옮겨야 한다.

## [0.5.1] - 2026-05-27

### 변경 — History 상태 표시와 접근성

- 성공·실패 아이콘과 중복되던 배지의 체크·엑스 문자를 제거하고 배지는 시각·소요 시간만 보여 준다.
- 스크린리더 라벨에는 성공·실패·실행 중·열림 상태를 지역화된 텍스트로 유지한다.

## [0.5.0] - 2026-05-14

### 추가 — 병렬 실행과 Task DAG 정식 지원

- parallel과 dependsOn, 결과 참조에서 DAG를 구성하고 최대 동시 실행 수를 설정할 수 있다. 기본 직렬
  실행은 유지한다.
- self·missing·cycle 의존성을 실행 전과 Doctor·Preview에서 검증하고 활성 OS 분기와 특수 입력 참조를
  정확히 추론한다.
- 동시 실행 태스크를 Actions 패널·터미널·verbose 로그에서 task id별로 구분한다.
- 병렬 실패는 모든 원인을 보존하고, prompt는 직렬화하며 Stop·timeout은 태스크별 프로세스를 추적한다.

## [0.4.44] - 2026-05-14

### 수정 — DAG 입력 의존성과 진행 라벨

- inputs의 bare task id도 자동 의존성으로 추론한다.
- 태스크가 선언 순서와 다르게 끝나도 Actions 패널은 실제 선언 번호를 표시한다.

## [0.4.43] - 2026-05-14

### 변경 — 병렬 실행 진행 상태

- 동시에 실행 중인 모든 태스크를 Actions 패널에 요약하고 lifecycle 전환마다 정확히 갱신한다.

## [0.4.42] - 2026-05-14

### 수정 — 병렬 로그와 전방 참조

- verbose 로그에 task id를 붙여 병렬 출력을 구분한다.
- Doctor와 Preview가 아직 실행되지 않은 정상 태스크 참조를 미해결로 오진하지 않게 했다.

## [0.4.41] - 2026-05-14

### 추가 — 병렬 실행과 Task DAG

- parallel·dependsOn·결과 참조로 실행 그래프를 만들고 self·missing·cycle을 시작 전에 거부한다.
- 실패·출력·터미널·프로세스 상태를 태스크별로 격리하고 대화형 prompt는 직렬화한다.
- 동시 실행 수 설정과 Preview 그래프 보고를 추가했으며 기본 직렬 실행은 유지한다.

## [0.4.40] - 2026-05-13

### 추가 — TaskHub Doctor

- JSON·스키마·중복 id·정규식·capture·diagnostics·미해결 변수·외부 쓰기·DAG 오류를 한 번에 검사하는
  Action Lint 명령을 추가했다.

## [0.4.39] - 2026-05-12

### 수정 — Windows PATH 조합

- 실행 파일 후보 경로를 호스트 OS가 아니라 path.win32 규칙으로 조합해 크로스플랫폼 판정을 바로잡았다.

## [0.4.38] - 2026-05-12

### 수정 — Windows 명령 인용

- 직접 실행 가능한 Windows 명령과 PowerShell이 필요한 스크립트를 구분한다.
- one-shot과 셸 태스크의 인자를 Windows 규칙에 맞게 인용하고 CRLF 기반 테스트를 보강했다.

## [0.4.37] - 2026-05-12

### 추가 — JSON Editor History 기록

- JSON 파일 열람을 History에 기록하고 컨텍스트 메뉴의 URI 인자 형태를 일관되게 정규화했다.

## [0.4.36] - 2026-05-12

### 추가 — Memory Map·Hex History 기록

- 액션 실행뿐 아니라 Memory Map과 Hex 파일 열람도 공용 History 모델에 기록한다.

## [0.4.35] - 2026-05-11

### 변경 — Memory Map 검색 탐색

- 이전·다음 매치 이동, 비매치 영역 숨김, 영역별 매치 수, 접힌 영역 자동 펼침을 추가했다.
- 정렬 뒤에도 현재 매치와 탐색 순서를 다시 동기화한다.

## [0.4.34] - 2026-05-11

### 변경 — Memory Map 검색 가시성

- 매치 텍스트 강조, 첫 결과 자동 스크롤, 고정 검색창과 정확한 개수 표시를 추가했다.
- 검색 중 필요한 Section·Function 열을 자동으로 보여 준다.

## [0.4.33] - 2026-05-07

### 수정 — 액션·즐겨찾기 데이터 보호

- 깨진 actions.json을 Preset Replace가 동의·백업 없이 덮어쓰지 못하게 했다.
- 즐겨찾기 중복, preset id 덮어쓰기, History 즉시 삭제에 확인과 명확한 안내를 추가했다.

## [0.4.32] - 2026-05-05

### 수정 — Links·Favorites 추가 흐름과 깨진 JSON 보호

- 깨진 links.json·favorites.json을 추가·편집·삭제가 새 파일로 덮어쓰지 않게 했다.
- 링크와 즐겨찾기 추가 단계를 줄이고 URL 검증·다중 선택·완료 알림을 일관되게 적용했다.

## [0.4.31] - 2026-05-05

### 변경 — 액션 생성 마법사 간소화

- 핵심 입력을 먼저 받고 선택지가 하나뿐인 destination 단계는 자동으로 건너뛴다.
- 깨진 actions.json을 바로 열 수 있는 복구 경로와 생성 후 추가 설정 안내를 제공한다.

## [0.4.30] - 2026-05-05

### 추가 — JSON Editor 데이터 보호·Undo·외부 변경 복구

- 편집 중인 셀이 유효하지 않으면 저장·행 이동·타입 변환을 막아 stale 데이터가 기록되지 않게 했다.
- Undo/Redo, Find 위젯, 정확한 saved baseline 비교를 추가해 수동으로 원래 값에 돌아오면 clean 상태가
  복원된다.
- 패널 종료 직전까지 미커밋 입력을 draft로 캡처하고, primitive·배열·객체 타입을 보존한 복구
  스냅샷을 파일별로 저장한다.
- 탭 전환·Reload·행 삭제·드래그·배열 추가·변환 전에 활성 셀을 commit하며, 취소한 입력은 host의
  dirty·recovery 상태에서도 제거한다.
- 외부 변경과 atomic replace를 감지해 Reload·Keep current edits·Discard를 제공한다. Keep은 새 디스크
  baseline만 갱신하고 현재 편집의 root 형태와 dirty 상태를 보존한다.
- 저장 응답·snapshot flush·dispose가 겹쳐도 순서를 보장하고, 이전 세션의 pending timer와 cache가
  새 파일의 상태를 바꾸지 못하게 했다.
- 파일 읽기·stat·크기·JSON parse 실패 뒤에도 복구본을 먼저 제안하며, baseline을 알 수 없는 상태는
  사용자 데이터와 충돌하지 않는 별도 sentinel로 표현한다.
- 수동·자동 Reload 모두 파일 크기 상한을 적용하고 stale mtime·깨진 glob·빈 열 이름 같은 경계
  문제를 정리했다.

## [0.4.29] - 2026-05-05

### 수정 — Memory Map 리포트·보안·영역 UX

- 웹뷰 script 데이터의 종료 태그 주입을 막고 Base64 클립보드 문자열을 UTF-8로 복원한다.
- Memory Region의 Base 주소를 실제 origin으로 표시한다.
- 영역 상세를 단일 펼침 토글로 통합하고, 요약 보고서와 전체 dump 복사를 분리했다.
- 웹뷰에서 Ctrl/Cmd+F로 검색창에 바로 이동할 수 있다.

## [0.4.28] - 2026-05-05

### 추가 — Run Any Action 팔레트

- 모든 액션을 fuzzy 검색·실행하고 유효한 최근 사용 항목을 먼저 보여 준다.
- recentLimit 설정을 추가하고 stale 항목 필터·0개 구분선·깨진 actions.json fallback을 정리했다.

## [0.4.26] - 2026-05-04

### 변경 — 동명 액션 History 구분

- 같은 title이 충돌할 때만 폴더 경로를 붙이고, 경로까지 같거나 없는 경우 action id를 덧붙인다.
- 표시 이름은 실행 시점에 저장해 이후 액션 이동에도 과거 기록을 안정적으로 식별한다.

## [0.4.25] - 2026-05-02

### 수정 — envPick과 실제 셸 환경 정합성

- 셸에서 읽을 수 없는 확장 host 전용 환경변수를 선택 목록에서 제외한다.
- probe 자체도 정제된 환경으로 실행해 상속 때문에 필터가 무력화되지 않게 했다.

## [0.4.24] - 2026-05-01

### 추가 — Markdown Preview와 HTML 브라우저 열기

- Explorer·편집기·Source Control에서 Markdown Preview와 기본 브라우저 열기를 제공한다.
- 각 메뉴가 넘기는 인자를 URI로 정규화해 다른 파일을 여는 일을 막았다.
- SCM 메뉴는 설정으로 숨길 수 있으며, 사용하지 않는 Simple Browser 명령은 제거했다.

## [0.4.23] - 2026-05-01

### 추가 — 액션별 키바인딩

- id가 있는 액션을 고유한 VS Code command로 등록하고 컨텍스트 메뉴에서 단축키를 지정할 수 있다.
- 시작 직후에도 등록되며, percent encoding으로 id 충돌을 막고 액션 변경에 따라 command를 diff-sync한다.
- 설정 파싱 실패 때 기존 command를 보존하고 실행 오류 알림을 한 번만 표시한다.

## [0.4.22] - 2026-05-01

### 추가 — Problems 진단 통합

- shell·command 출력에 GCC·MSVC 프리셋이나 사용자 정규식을 적용해 Problems 진단으로 변환한다.
- 성공·실패 양쪽의 stdout·stderr를 검사하고 보간된 cwd를 기준으로 상대 파일을 해석한다.
- 같은 액션의 여러 태스크가 같은 파일에 낸 진단을 모두 보존하며 액션 재실행 시 이전 진단을 지운다.
- 첫 버전은 캡처 모드의 단일 라인 matcher만 지원한다.

## [0.4.21] - 2026-04-30

### 추가 — 멀티 태스크 진행 표시

- Actions 카드에 현재 태스크 번호·이름을 표시하고 pipeline transition 이벤트로 상태를 갱신한다.
- UI 콜백 실패는 실제 실행 파이프라인과 격리한다.

## [0.4.20] - 2026-04-30

### 추가 — History 마지막 실행 배지

- 실행 시각과 소요 시간을 지역화해 표시하고 시작·종료 시간을 History 모델에 저장한다.
- 자정 경계와 음수 시간 같은 표시 오류를 방어한다.

## [0.4.19] - 2026-04-30

### 추가 — 저장된 입력으로 History 재실행

- 대화형 태스크 입력을 History에 저장하고 별도 명령으로 prompt 없이 다시 실행한다.
- 비밀번호는 저장하지 않으며 재실행도 원래 태스크의 공통 후처리를 거친다.
- 이번 버전은 저장값 그대로 재사용하는 모드만 제공한다.

## [0.4.18] - 2026-04-24

### 수정 — 가져오기·Preset·JSON·실행 안정성 종합 보강

- 깨진 actions.json을 가져오기나 Preset Replace가 덮어쓰지 않도록 검증·백업·동의 흐름을 추가했다.
- preset 우선 병합과 액션·태스크 중복 id 검사를 실제 선택 설명과 맞췄다.
- shell 출력 캡처 상한을 두고 overflow를 사용자 중지가 아닌 실제 실패로 기록한다.
- JSON Editor 문자열 타입과 dirty 재열기 확인을 보장하고, Favorites 경로를 워크스페이스 상대값으로
  저장한다.
- 멀티루트의 import·export와 액션별 폴더 선택을 통일하고 인자 없이는 쓸 수 없는 command를
  팔레트에서 숨겼다.
- 핵심 상한과 실제 provider·웹뷰 경로를 경계값 테스트로 고정했다.

## [0.4.17] - 2026-04-23

### 수정 — 가져오기·JSON Editor·멀티루트 경계

- 가져오는 폴더 액션의 모든 하위 id를 검사해 기존 액션과 충돌하는 묶음을 저장 전에 건너뛴다.
- JSON Editor가 다른 파일·Reload·저장 실패에서 dirty 상태와 확인 절차를 정확히 유지한다.
- Preview·내보내기·가져오기·Memory Map이 액션 또는 사용자가 고른 실제 워크스페이스를 사용한다.

## [0.4.16] - 2026-04-23

### 정리 — provider 호환 shim 제거

- 이미 providers 디렉터리로 옮긴 클래스의 재export와 사용하지 않는 import를 제거했다.

## [0.4.15] - 2026-04-23

### 정리 — 오류 로그를 OutputChannel로 통일

- 남아 있던 console.error를 TaskHub OutputChannel로 옮기고 non-Error throw도 안전하게 문자열화한다.

## [0.4.14] - 2026-04-23

### 정리 — JSON Editor 미러 동기화 문서

- 웹뷰 스크립트와 테스트용 jsonEditorUtils 복사본이 함께 갱신돼야 하는 위치를 양쪽에 명시했다.

## [0.4.13] - 2026-04-23

### 문서 — 예제 정리와 README 리디자인

- 루트의 hover 샘플을 examples로 옮기고 VSIX 제외 규칙을 정리했다.
- 한국어 README를 재구성하고 같은 구조의 영문 README·스크린샷·예제 안내를 추가했다.

## [0.4.12] - 2026-04-22

### 추가 — 파일 쓰기와 태스크 제어 옵션

- writeFile·appendFile 태스크에 encoding·EOL·overwrite·mkdirs와 워크스페이스 경계 검사를 추가했다.
- 모든 태스크에 timeoutSeconds와 continueOnError를 적용했다.
- Preview·스키마·문서와 통합 테스트를 새 실행 계약에 맞췄다.

## [0.4.11] - 2026-04-22

### 문서 — README 사이드바 이미지 크기

- 긴 사이드바 스크린샷을 읽기 쉬운 폭으로 제한했다.

## [0.4.10] - 2026-04-22

### 개선 — 사라진 즐겨찾기 파일 처리

- 대상 파일이 없으면 이해하기 쉬운 안내와 즐겨찾기 제거 동작을 제공한다.
- 동일 항목 하나만 정확히 삭제하는 공용 identity 규칙을 사용한다.

## [0.4.9] - 2026-04-22

### 문서 — README 스크린샷과 Hex 샘플

- 주요 패널·호버·편집기 스크린샷과 Hex Viewer용 1KB 예제 바이너리를 추가했다.

## [0.4.8] - 2026-04-22

### 변경 — 메인 뷰 이름을 Actions로 변경

- 표시 이름과 관련 문서를 실제 역할에 맞게 바꾸고 내부 view id는 유지했다.

## [0.4.7] - 2026-04-22

### 변경 — 기본 제공 링크 정비

- VS Code Docs 링크를 제거하고 Claude와 GitHub 링크를 추가했다.

## [0.4.6] - 2026-04-21

### 추가 — ZIP/Unzip 내장 엔진

- tool을 생략한 zip·unzip 태스크는 번들 JS 엔진으로 .zip을 처리한다.
- 추출 전 zip-slip 경로를 차단하고 다른 확장자는 외부 tool을 요구한다.
- Preview·스키마·문서를 내장·외부 엔진 양쪽에 맞췄다.

## [0.4.5] - 2026-04-20

### 추가 — 안전한 환경변수 조회 액션

- envPick이 값은 노출하지 않고 환경변수 이름을 선택해 결과로 돌려준다.
- 선택한 한 변수만 출력하거나 이름을 직접 입력하는 기본 액션을 제공한다.

## [0.4.4] - 2026-04-19

### 보안 — 워크스페이스 신뢰 경계 강화

- 파일 유래 hover에서 trusted Markdown을 제거하고 외부 링크를 http·https·mailto로 제한했다.
- Favorite 파일 경로를 워크스페이스 안으로 제한하고 전체 환경변수 dump 기본 액션을 제거했다.
- 신뢰하지 않은 워크스페이스에서는 확장을 비활성화한다.

## [0.4.3] - 2026-04-17

### 테스트 — Archive·Terminal·Lifecycle·Error 시나리오

- 아카이브, 출력 모드, 액션 상태·History, 실패 정리 경로의 통합 테스트를 추가했다.
- 실제 executeAction 진입점을 테스트에서 호출할 수 있게 공개했다.

## [0.4.1] - 2026-04-17

### 테스트 — 명령·워크스페이스·대화형 태스크 시나리오

- command 실행과 워크스페이스 안전, quickPick·inputBox·fileDialog 파이프라인 통합 테스트를 추가했다.

## [0.4.0] - 2026-04-17

### 추가 — 출력 Capture와 Preview Run

- shell·command·stringManipulation 출력에서 정규식이나 라인 번호로 이름 있는 값을 추출해 후속
  태스크가 참조할 수 있다.
- 예약·중복 이름과 잘못된 정규식을 거부하며 shell·command 캡처에는 결과 전달 설정을 요구한다.
- Preview Run이 실행 없이 command·cwd·env·출력 경로·capture·미해결 변수·외부 쓰기를 보여 준다.
- 파일 출력 overwrite 기본값과 문자열 보간 결과도 실제 런타임 규칙으로 표시한다.

## [0.3.22] - 2026-04-17

### 성능 — 확장 활성화 경량화

- C/C++ 파일을 열 때 hover가 바로 활성화되도록 activation event를 추가했다.
- actions 스키마 컴파일과 액션 로드를 캐시하고 설정·프리셋 변경 때만 무효화한다.
- 링크·즐겨찾기·History는 뷰를 처음 열 때 읽고, 프로덕션의 불필요한 번들 파일 watcher를 제거했다.

## [0.3.21] - 2026-04-17

### 정리 — History Provider 모듈 분리

- History 모델과 TreeDataProvider를 providers 디렉터리로 옮기고 기존 export 호환성을 유지했다.

## [0.3.20] - 2026-04-17

### 정리 — Favorites Provider 모듈 분리

- 즐겨찾기 모델·정규화·TreeDataProvider를 extension.ts에서 독립 모듈로 옮겼다.

## [0.3.19] - 2026-04-17

### 정리 — Links Provider 모듈 분리

- 링크 모델·정규화·TreeDataProvider를 독립 모듈로 옮기고 순환 import를 제거했다.

## [0.3.18] - 2026-04-17

### 정리 — Actions Provider 모듈 분리

- Actions 트리와 공용 실행 상태를 별도 provider·actionStatus 모듈로 옮겼다.

## [0.3.17] - 2026-04-17

### 정리 — Pipeline 유틸리티 모듈 분리

- VS Code API에 의존하지 않는 보간·출력·경로 함수 13개를 pipelineUtils로 옮겨 직접 단위 테스트한다.

## [0.3.16] - 2026-04-17

### 성능 — Hover 파일 I/O 비동기화

- taskhub_types.json 읽기와 realpath·stat을 비동기 API로 바꿔 느린 파일시스템에서 extension host를
  막지 않게 했다.

## [0.3.15] - 2026-04-17

### 개선 — Linker 파서 진단 계약

- 기존 빈 결과 API는 유지하면서 실패 원인을 warnings로 돌려주는 parseLinkerFileWithDiagnostics를
  추가하고 파서의 모호한 null 계약을 문서화했다.

## [0.3.14] - 2026-04-17

### 수정 — 오류와 확인 문구 지역화

- 액션 소스 로드 실패에 문맥을 붙이고 기본 confirm·관련 사용자 메시지를 한영으로 제공한다.

## [0.3.13] - 2026-04-17

### 수정 — CSP·희소 Hex·출력 경로

- Memory Map 컨트롤을 nonce 기반 CSP와 호환되게 했다.
- HEX/SREC의 과도한 희소 주소 범위를 거부하고 상대 filePath를 워크스페이스 기준으로 해석한다.
- CSP nonce 생성에 암호학적 난수를 사용한다.

## [0.3.12] - 2026-04-17

### 보안 — 입력·파서·Hover·웹뷰 방어 강화

- 보간값의 null byte·32KB 상한·비문자 타입을 검사하고 파일 출력은 워크스페이스 내부로 제한한다.
- ELF·HEX/SREC·매크로·struct 파서에 크기·범위·복잡도 상한을 추가했다.
- 모든 LSP hover에 timeout·재진입 방지·라인 길이 상한·LRU 캐시를 적용했다.
- Hex·JSON·Memory Map 웹뷰에 nonce CSP를 적용하고 오류 HTML을 escape한다.
- Memory Map 증분 검색과 워크스페이스 watcher debounce를 추가했다.

## [0.3.11] - 2026-04-17

### 수정 — enum 식별자 수식 계산

- 다른 enum 이름, 단순 이항식, 한 단계 괄호와 주석이 포함된 상수 값을 계산한다.

## [0.3.10] - 2026-04-17

### 수정 — 큰 enum의 hover fallback

- 100줄 제한을 없애 enum 시작과 닫는 중괄호까지 탐색해 수백 개 항목의 암묵값도 계산한다.

## [0.3.9] - 2026-04-07

### 개선 — Memory Map 다중 패널

- 서로 다른 파일은 별도 탭으로 열고 같은 경로만 재사용한다.
- Go to Symbol은 마지막 활성 패널을 대상으로 하며 Node·Mocha 타입 설정도 바로잡았다.

## [0.3.8] - 2026-04-07

### 성능 — Memory Map 지연 렌더링

- 영역 상세는 펼칠 때 만들고 200행 초과 표는 가상 스크롤한다.
- 검색·정렬을 DOM 대신 원본 데이터에서 처리하고 상단 이동 버튼을 복구했다.

## [0.3.7] - 2026-04-07

### 수정 — ARM Listing 함수명과 End 주소

- 괄호 없는 오브젝트 행에서도 함수명을 추출하고 End 주소를 포함 범위로 표시한다.

## [0.3.6] - 2026-04-07

### 개선 — 예제 액션 발견성

- 버전 항목에서 CHANGELOG를 열고 예제 JSON 버튼을 제목 표시줄로 옮겼다.
- command·confirm·shell 옵션·dialog 결과·password·stringManipulation 함수 예제를 보강했다.

## [0.3.5] - 2026-04-06

### 추가 — Memory Map HTML 저장

- 현재 펼침·검색 상태를 독립 실행 가능한 HTML로 저장해 공유·보관할 수 있다.

## [0.3.4] - 2026-04-03

### 수정 — Object Summary 섹션·함수 표시

- 상세 행의 Section 값을 올바르게 표시하고 미등록 prefix의 함수명도 추출한다.

## [0.3.3] - 2026-04-03

### 개선 — Memory Map 상세 열과 정렬

- Section·Function·End Address와 오브젝트별 상세를 복원하고 Object Summary를 접을 수 있게 했다.
- 수치 열의 첫 정렬 방향과 숨은 상세 행 토글을 바로잡았다.

## [0.3.2] - 2026-04-03

### 추가 — ELF 기반 Memory Map 분석

- ELF 프로그램 헤더와 심볼 테이블로 FLASH·RAM 영역, 함수·변수 크기와 빈 공간을 분석한다.
- 영역별 오브젝트 점유율과 Code·RO·RW·ZI 세부 정보, Function 열을 제공한다.

## [0.3.0] - 2026-04-02

### 변경 — 지역화·패널 격리·파일 상한

- 사용자 메시지를 VS Code 언어에 따라 한국어·영어로 표시한다.
- JSON·Hex·Memory Map 패널의 핸들러와 저장 대상을 파일별로 격리한다.
- 중복 action id는 전체 로드를 막지 않고 경고하며 포인터·전방 타입 파싱을 보강했다.
- Hex 50MB, Memory Map 100MB, JSON 10MB 상한과 자세한 읽기·파싱 오류를 추가했다.

## [0.2.52] - 2026-04-02

### 수정 — Memory Map 사용량·정렬·탐색

- 겹친 섹션과 alignment padding 때문에 free·used가 부풀거나 최대 크기를 넘던 계산을 바로잡았다.
- Size를 실제 byte 기준으로 정렬하고 축소 화면에서도 used/free 비율을 유지한다.
- ELF와 ARM Listing 화면을 통일하고 영역 이동·상세 정렬·맨 위로 이동을 추가했다.

## [0.2.50] - 2026-04-02

### 성능 — 대용량 Hex Viewer

- 바이너리가 SREC로 잘못 감지되던 정규식 문제를 고쳤다.
- Uint8Array와 가상 스크롤을 사용해 16MB 이상 파일의 Map 한계와 웹뷰 정지를 줄였다.

## [0.2.47] - 2026-04-01

### 추가 — Hex Viewer

- Intel HEX·Motorola SREC·Raw Binary를 자동 감지해 hex dump로 표시한다.
- 1·2·4·8바이트 unit, endian 전환, 패턴 검색, 주소 이동, 선택값 해석과 gap 표시를 제공한다.

## [0.2.46] - 2026-04-01

### 추가 — 액션 Import·Export 진입점

- 액션·폴더 컨텍스트 메뉴에서 개별 내보내기를 지원하고 제목 표시줄에 Import 버튼을 추가했다.
- 가져온 뒤 Actions 뷰를 즉시 갱신한다.

## [0.2.45] - 2026-04-01

### 변경 — Memory Map Overview

- 링커 보고값과 직접 계산한 Used·Free를 별도 열로 표시한다.
- 영역 단위 이동과 전체 펼침·접힘을 추가하고 중복 요약 카드는 제거했다.

## [0.2.44] - 2026-04-01

### 추가 — Memory Map 검색·영역 탐색

- 섹션 이름·주소·타입 검색과 영역 QuickPick 이동을 추가했다.
- 영역 요약, 기본 접힘, 매치 자동 펼침과 실제 execution region 기준 집계로 중복 계산을 줄였다.

## [0.2.43] - 2026-04-01

### 추가 — ARM Linker Listing 분석

- ARM Compiler 5·6 listing에서 실행 영역과 섹션·오브젝트 기여도를 파싱한다.
- 빈 공간과 세그먼트 레이아웃, 주소·타입·Free 보고서를 표시한다.

## [0.2.42] - 2026-03-31

### 추가 — Memory Map Viewer와 JSON Editor 개선

- ELF32, GNU linker script, ARM scatter file로 FLASH·RAM 사용량과 섹션을 시각화한다.
- Cortex-R/M과 양쪽 endian을 지원한다.
- JSON Editor가 루트 배열과 중첩 객체 편집을 지원하고 불필요한 타입 변환 UI를 정리했다.

## [0.2.40] - 2026-03-31

### 추가 — confirm 태스크와 액션 공유

- 메시지·버튼 라벨·변수 보간을 지원하는 confirm 태스크로 파이프라인을 안전하게 취소할 수 있다.
- 워크스페이스 액션을 taskhub 또는 raw JSON 형식으로 Import·Export하며 id와 스키마를 검증한다.

## [0.2.36] - 2026-03-18

### 유지보수 — 의존성 보안 업데이트

- serialize-javascript override와 개발 의존성을 갱신하고 불필요한 문서를 정리했다.

## [0.2.35] - 2026-02-19

### 성능·보안 — watcher·캐시·의존성 정리

- debounce에 cancel을 추가해 watcher 해제 뒤 지연 작업이 실행되지 않게 했다.
- 타입 설정의 absent·mtime 캐시와 정규식 상수 재사용으로 파일 I/O와 할당을 줄였다.
- npm-run-all2와 보안 override로 알려진 의존성 취약점을 해결했다.

## [0.2.34] - 2026-02-19

### 수정 — 타입·파서·설정 항목 정확성

- 커스텀 타입을 기본 타입과 병합하고 union 중괄호 파싱이 문자열·주석을 무시하게 했다.
- 즐겨찾기는 엄격한 identity로 삭제하고 Preset Keep both 문구를 실제 동작에 맞췄다.
- ESLint·TypeScript·Mocha 테스트 도구를 호환 버전으로 갱신했다.

## [0.2.33] - 2026-02-09

### 수정 — 파서와 Hover 안정성

- 매크로 순환 실패 뒤 상태를 항상 정리하고 LSP 요청에 3초 timeout을 적용했다.
- 레지스터·struct 파서는 주석과 문자열 속 중괄호를 무시한다.
- 비활성화 시 전역 실행 상태를 정리한다.

## [0.2.32] - 2026-01-20

### 추가 — 프로젝트별 타입 설정

- taskhub_types.json에서 타입 크기·alignment·packingAlignment를 정의하고 스키마 자동완성을 제공한다.

## [0.2.31] - 2026-01-20

### 추가 — Windows·커스텀 타입 크기 계산

- Windows 8·16·32·64비트 기본 타입 크기를 지원한다.
- 문서의 struct·class를 여러 번 순회해 중첩·전방 참조 타입을 등록하고 중복·선언만 있는 타입을
  안전하게 처리한다.
- 프로젝트별 커스텀 타입 설정과 스키마를 함께 추가했다.

## [0.2.30] - 2026-01-16

### 수정 — 32비트 필드·Hover·Preset 병합

- 32비트 전체 레지스터와 SFR 마스크의 JavaScript shift wrap을 바로잡고 범위를 검증한다.
- 같은 줄의 여러 값 중 요청한 심볼 값만 Hover에 사용한다.
- 워크스페이스 없는 cwd를 생략하고 Preset Keep both의 중복 id를 재귀적으로 걸러 낸다.
- dialog options와 inputs 타입을 구체화했다.

## [0.2.29] - 2026-01-15

### 개선 — SFR Access Type 설명

- RO·RW·W1C 등 접근 타입 약어의 의미를 hover tooltip에 표시한다.

## [0.2.28] - 2026-01-12

### 수정 — 수동 중지 History 상태

- Stop 버튼으로 종료한 액션의 History가 즉시 올바른 상태로 갱신된다.

## [0.2.27] - 2026-01-04

### 추가 — Preset 시스템

- 확장·워크스페이스 Preset을 찾아 적용하고 현재 액션을 새 Preset으로 저장한다.
- 여러 빌드·테스트 환경의 액션 구성을 팀과 공유할 수 있다.

## [0.2.26] - 2026-01-03

### 첫 공개 릴리스

- 사용자 정의 액션·파이프라인, 링크·즐겨찾기, History와 액션 마법사를 제공한다.
- Number Base·SFR Bit Field·실험적 Bit Operation Hover를 포함한다.
- 멀티루트 워크스페이스, 검색·그룹화와 JSON 스키마 검증을 지원한다.

## [0.2.10] - 2024-11-08

### 수정 — 활성화·멀티루트·중지 안정성

- 모든 뷰와 명령의 activation event를 명시하고 TypeScript 호환 선언을 보강했다.
- actions·links·favorites를 멀티루트별로 감시하고 대상 워크스페이스를 선택하게 했다.
- 캡처 모드 프로세스도 Stop·Stop All로 종료할 수 있게 추적한다.

## [0.1.0]

### 첫 릴리스

- TaskHub의 초기 기능을 공개했다.
