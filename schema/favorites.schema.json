{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TaskHub Favorites",
  "description": "A list of favorite files for quick access.",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "The display name for the favorite file."
      },
      "path": {
        "type": "string",
        "description": "The absolute path to the file. Can use `${workspaceFolder}`."
      },
      "line": {
        "type": "integer",
        "minimum": 1,
        "description": "Optional 1-based line number to open."
      },
      "group": {
        "type": "string",
        "description": "Optional group label. Favorites with the same group appear under a collapsible section."
      },
      "tags": {
        "type": "array",
        "description": "Optional tags to help filter favorites.",
        "items": {
          "type": "string"
        }
      }
    },
    "required": ["title", "path"]
  }
}
