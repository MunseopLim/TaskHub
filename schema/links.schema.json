{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TaskHub Links",
  "description": "A list of useful web links.",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "The display name for the link."
      },
      "link": {
        "type": "string",
        "description": "The URL to open.",
        "format": "uri"
      },
      "group": {
        "type": "string",
        "description": "Optional group label. Links with the same group appear under a collapsible section."
      },
      "tags": {
        "type": "array",
        "description": "Optional tags to help filter links.",
        "items": {
          "type": "string"
        }
      }
    },
    "required": ["title", "link"]
  }
}
